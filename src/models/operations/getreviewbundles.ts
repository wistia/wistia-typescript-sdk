/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Field to order by. The default is id.
 */
export const GetReviewBundlesSortBy = {
  Id: "id",
  Name: "name",
  Created: "created",
  Updated: "updated",
} as const;
/**
 * Field to order by. The default is id.
 */
export type GetReviewBundlesSortBy = ClosedEnum<typeof GetReviewBundlesSortBy>;

/**
 * Direction to order by. (0 = desc, 1 = asc; default is 1)
 */
export const GetReviewBundlesSortDirection = {
  Zero: 0,
  One: 1,
} as const;
/**
 * Direction to order by. (0 = desc, 1 = asc; default is 1)
 */
export type GetReviewBundlesSortDirection = ClosedEnum<
  typeof GetReviewBundlesSortDirection
>;

/**
 * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
 *
 * @remarks
 * values is ignored if `cursor[before]` or `cursor[after]` are set.
 */
export const GetReviewBundlesEnabled = {
  Zero: 0,
  One: 1,
} as const;
/**
 * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
 *
 * @remarks
 * values is ignored if `cursor[before]` or `cursor[after]` are set.
 */
export type GetReviewBundlesEnabled = ClosedEnum<
  typeof GetReviewBundlesEnabled
>;

/**
 * If `cursor[enabled]` is set to 1 then cursor pagination is enabled and the
 *
 * @remarks
 * first set of records are fetched up to the `per_page`. Cursor
 * pagination will also be turned on if `cursor[before]` or `cursor[after]`
 * are set. Records returned will have a `cursor` property set which can be used to fetch more records in the same `sort_by` ordering.
 * The cursor value of the last record can be used to fetch records after the current result set and
 * the cursor of the first record can be used to fetch records before the result set.
 *
 * NOTE: a cursor value is only valid if the `sort_by` value hasn't changed from the
 * last fetch. For example, you cannot fetch using `sort_by` id and then pass that
 * cursor value to a `sort_by` name.
 */
export type GetReviewBundlesCursor = {
  /**
   * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
   *
   * @remarks
   * values is ignored if `cursor[before]` or `cursor[after]` are set.
   */
  enabled?: GetReviewBundlesEnabled | undefined;
  /**
   * If `cursor[before]` is set then cursor pagination is enabled and all records
   *
   * @remarks
   * before the cursor up to the `per_page` are returned. This feature is useful for
   * fetching "new records", for example, in a "pull to refersh" feature when showing records in a descending
   * order.
   */
  before?: string | undefined;
  /**
   * If `cursor[after]` is set then cursor pagination is enabled and all records
   *
   * @remarks
   * after the cursor up to the `per_page` are returned.
   */
  after?: string | undefined;
};

export type GetReviewBundlesRequest = {
  /**
   * Restrict the results to the review bundles with these hashed IDs.
   */
  hashedIds?: Array<string> | undefined;
  /**
   * Restrict the results to review bundles whose name contains this value (case-insensitive).
   */
  name?: string | undefined;
  /**
   * Restrict the results to review bundles that include the media with this hashed ID.
   */
  mediaHashedId?: string | undefined;
  /**
   * Restrict the results to review bundles that include any media from the folder with this hashed ID.
   */
  folderHashedId?: string | undefined;
  /**
   * Field to order by. The default is id.
   */
  sortBy?: GetReviewBundlesSortBy | undefined;
  /**
   * Direction to order by. (0 = desc, 1 = asc; default is 1)
   */
  sortDirection?: GetReviewBundlesSortDirection | undefined;
  /**
   * The page number to retrieve. This cannot be combined with `cursor`,
   *
   * @remarks
   * pagination.
   */
  page?: number | undefined;
  /**
   * The number of medias per page. Use this for both offset pagination and cursor pagination.
   */
  perPage?: number | undefined;
  /**
   * If `cursor[enabled]` is set to 1 then cursor pagination is enabled and the
   *
   * @remarks
   * first set of records are fetched up to the `per_page`. Cursor
   * pagination will also be turned on if `cursor[before]` or `cursor[after]`
   * are set. Records returned will have a `cursor` property set which can be used to fetch more records in the same `sort_by` ordering.
   * The cursor value of the last record can be used to fetch records after the current result set and
   * the cursor of the first record can be used to fetch records before the result set.
   *
   * NOTE: a cursor value is only valid if the `sort_by` value hasn't changed from the
   * last fetch. For example, you cannot fetch using `sort_by` id and then pass that
   * cursor value to a `sort_by` name.
   */
  cursor?: GetReviewBundlesCursor | undefined;
};

/**
 * A machine-readable identifier for the specific authorization failure.
 */
export const GetReviewBundlesCode = {
  UnauthorizedCredentials: "unauthorized_credentials",
  AccountInactive: "account_inactive",
  UnauthorizedScope: "unauthorized_scope",
  UnauthorizedParams: "unauthorized_params",
} as const;
/**
 * A machine-readable identifier for the specific authorization failure.
 */
export type GetReviewBundlesCode = ClosedEnum<typeof GetReviewBundlesCode>;

export type GetReviewBundlesResponse = {
  /**
   * A unique alphanumeric identifier for this review bundle.
   */
  hashedId?: string | undefined;
  /**
   * The bundle display name.
   */
  name?: string | undefined;
  /**
   * The URL of the bundle.
   */
  url?: string | undefined;
  /**
   * Whether the videos in the bundle can be downloaded.
   */
  allowDownloads?: boolean | undefined;
  /**
   * A cursor for stable pagination based on current `sort_by` order. You can pass this to `cursor[before]` or `cursor[after]` as a parameter to fetch the records before or after this record in the same sort order. This is only populated if records were fetched with `cursor[enabled]`, or `cursor[before]` or `cursor[after]`.
   */
  cursor?: string | null | undefined;
};

/** @internal */
export const GetReviewBundlesSortBy$outboundSchema: z.ZodNativeEnum<
  typeof GetReviewBundlesSortBy
> = z.nativeEnum(GetReviewBundlesSortBy);

/** @internal */
export const GetReviewBundlesSortDirection$outboundSchema: z.ZodNativeEnum<
  typeof GetReviewBundlesSortDirection
> = z.nativeEnum(GetReviewBundlesSortDirection);

/** @internal */
export const GetReviewBundlesEnabled$outboundSchema: z.ZodNativeEnum<
  typeof GetReviewBundlesEnabled
> = z.nativeEnum(GetReviewBundlesEnabled);

/** @internal */
export type GetReviewBundlesCursor$Outbound = {
  enabled?: number | undefined;
  before?: string | undefined;
  after?: string | undefined;
};

/** @internal */
export const GetReviewBundlesCursor$outboundSchema: z.ZodType<
  GetReviewBundlesCursor$Outbound,
  z.ZodTypeDef,
  GetReviewBundlesCursor
> = z.object({
  enabled: GetReviewBundlesEnabled$outboundSchema.optional(),
  before: z.string().optional(),
  after: z.string().optional(),
});

export function getReviewBundlesCursorToJSON(
  getReviewBundlesCursor: GetReviewBundlesCursor,
): string {
  return JSON.stringify(
    GetReviewBundlesCursor$outboundSchema.parse(getReviewBundlesCursor),
  );
}

/** @internal */
export type GetReviewBundlesRequest$Outbound = {
  "hashed_ids[]"?: Array<string> | undefined;
  name?: string | undefined;
  media_hashed_id?: string | undefined;
  folder_hashed_id?: string | undefined;
  sort_by?: string | undefined;
  sort_direction?: number | undefined;
  page?: number | undefined;
  per_page?: number | undefined;
  cursor?: GetReviewBundlesCursor$Outbound | undefined;
};

/** @internal */
export const GetReviewBundlesRequest$outboundSchema: z.ZodType<
  GetReviewBundlesRequest$Outbound,
  z.ZodTypeDef,
  GetReviewBundlesRequest
> = z.object({
  hashedIds: z.array(z.string()).optional(),
  name: z.string().optional(),
  mediaHashedId: z.string().optional(),
  folderHashedId: z.string().optional(),
  sortBy: GetReviewBundlesSortBy$outboundSchema.optional(),
  sortDirection: GetReviewBundlesSortDirection$outboundSchema.optional(),
  page: z.number().int().optional(),
  perPage: z.number().int().optional(),
  cursor: z.lazy(() => GetReviewBundlesCursor$outboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    hashedIds: "hashed_ids[]",
    mediaHashedId: "media_hashed_id",
    folderHashedId: "folder_hashed_id",
    sortBy: "sort_by",
    sortDirection: "sort_direction",
    perPage: "per_page",
  });
});

export function getReviewBundlesRequestToJSON(
  getReviewBundlesRequest: GetReviewBundlesRequest,
): string {
  return JSON.stringify(
    GetReviewBundlesRequest$outboundSchema.parse(getReviewBundlesRequest),
  );
}

/** @internal */
export const GetReviewBundlesCode$inboundSchema: z.ZodNativeEnum<
  typeof GetReviewBundlesCode
> = z.nativeEnum(GetReviewBundlesCode);

/** @internal */
export const GetReviewBundlesResponse$inboundSchema: z.ZodType<
  GetReviewBundlesResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  hashed_id: z.string().optional(),
  name: z.string().optional(),
  url: z.string().optional(),
  allow_downloads: z.boolean().optional(),
  cursor: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "hashed_id": "hashedId",
    "allow_downloads": "allowDownloads",
  });
});

export function getReviewBundlesResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetReviewBundlesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetReviewBundlesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetReviewBundlesResponse' from JSON`,
  );
}
