/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetMediasMediaIdShareLinkRequest = {
  /**
   * The hashed ID of the media.
   */
  mediaId: string;
};

/**
 * A machine-readable identifier for the specific authorization failure.
 */
export const GetMediasMediaIdShareLinkCode = {
  UnauthorizedCredentials: "unauthorized_credentials",
  AccountInactive: "account_inactive",
  UnauthorizedScope: "unauthorized_scope",
  UnauthorizedParams: "unauthorized_params",
} as const;
/**
 * A machine-readable identifier for the specific authorization failure.
 */
export type GetMediasMediaIdShareLinkCode = ClosedEnum<
  typeof GetMediasMediaIdShareLinkCode
>;

/**
 * Controls who can view the media via this share link.
 *
 * @remarks
 *
 * - `unlocked`: anyone with the link can view the media.
 * - `account`: only signed-in members of the media's account can view.
 * - `locked`: only contacts with access to the media's folder can view.
 * - `domain_verified`: only viewers signed in with an email address at a
 *   domain verified on the media's account can view. Requires the account
 *   to be enrolled in the domain validation gate; otherwise setting this
 *   value returns 400.
 */
export const GetMediasMediaIdShareLinkVisibility = {
  Unlocked: "unlocked",
  Account: "account",
  Locked: "locked",
  DomainVerified: "domain_verified",
} as const;
/**
 * Controls who can view the media via this share link.
 *
 * @remarks
 *
 * - `unlocked`: anyone with the link can view the media.
 * - `account`: only signed-in members of the media's account can view.
 * - `locked`: only contacts with access to the media's folder can view.
 * - `domain_verified`: only viewers signed in with an email address at a
 *   domain verified on the media's account can view. Requires the account
 *   to be enrolled in the domain validation gate; otherwise setting this
 *   value returns 400.
 */
export type GetMediasMediaIdShareLinkVisibility = ClosedEnum<
  typeof GetMediasMediaIdShareLinkVisibility
>;

/**
 * A shareable link for a single media. Each media can have at most one
 *
 * @remarks
 * share link, and the link's `visibility` controls who can view the
 * media when visiting the link.
 */
export type GetMediasMediaIdShareLinkResponse = {
  /**
   * The hashed ID of the share link. This appears in the link's URL.
   */
  id: string;
  /**
   * The hashed ID of the media this share link points to.
   */
  mediaId: string;
  /**
   * The full URL viewers can use to view the media via this share link.
   */
  url: string;
  /**
   * The share link's custom slug, if one is set. When present, the slug
   *
   * @remarks
   * replaces the hashed ID in the link's URL.
   */
  slug: string | null;
  /**
   * Controls who can view the media via this share link.
   *
   * @remarks
   *
   * - `unlocked`: anyone with the link can view the media.
   * - `account`: only signed-in members of the media's account can view.
   * - `locked`: only contacts with access to the media's folder can view.
   * - `domain_verified`: only viewers signed in with an email address at a
   *   domain verified on the media's account can view. Requires the account
   *   to be enrolled in the domain validation gate; otherwise setting this
   *   value returns 400.
   */
  visibility: GetMediasMediaIdShareLinkVisibility;
  /**
   * When the share link was created.
   */
  createdAt: Date;
  /**
   * When the share link was last updated.
   */
  updatedAt: Date;
};

/** @internal */
export type GetMediasMediaIdShareLinkRequest$Outbound = {
  mediaId: string;
};

/** @internal */
export const GetMediasMediaIdShareLinkRequest$outboundSchema: z.ZodType<
  GetMediasMediaIdShareLinkRequest$Outbound,
  z.ZodTypeDef,
  GetMediasMediaIdShareLinkRequest
> = z.object({
  mediaId: z.string(),
});

export function getMediasMediaIdShareLinkRequestToJSON(
  getMediasMediaIdShareLinkRequest: GetMediasMediaIdShareLinkRequest,
): string {
  return JSON.stringify(
    GetMediasMediaIdShareLinkRequest$outboundSchema.parse(
      getMediasMediaIdShareLinkRequest,
    ),
  );
}

/** @internal */
export const GetMediasMediaIdShareLinkCode$inboundSchema: z.ZodNativeEnum<
  typeof GetMediasMediaIdShareLinkCode
> = z.nativeEnum(GetMediasMediaIdShareLinkCode);

/** @internal */
export const GetMediasMediaIdShareLinkVisibility$inboundSchema: z.ZodNativeEnum<
  typeof GetMediasMediaIdShareLinkVisibility
> = z.nativeEnum(GetMediasMediaIdShareLinkVisibility);

/** @internal */
export const GetMediasMediaIdShareLinkResponse$inboundSchema: z.ZodType<
  GetMediasMediaIdShareLinkResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  media_id: z.string(),
  url: z.string(),
  slug: z.nullable(z.string()),
  visibility: GetMediasMediaIdShareLinkVisibility$inboundSchema,
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  updated_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "media_id": "mediaId",
    "created_at": "createdAt",
    "updated_at": "updatedAt",
  });
});

export function getMediasMediaIdShareLinkResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetMediasMediaIdShareLinkResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetMediasMediaIdShareLinkResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetMediasMediaIdShareLinkResponse' from JSON`,
  );
}
