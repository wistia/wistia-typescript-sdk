/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Controls who can view the media via this share link.
 *
 * @remarks
 *
 * - `unlocked`: anyone with the link can view the media.
 * - `account`: only signed-in members of the media's account can view.
 * - `locked`: only contacts with access to the media's folder can view.
 * - `domain_verified`: only viewers signed in with an email address at a
 *   domain verified on the media's account can view. Requires the account
 *   to be enrolled in the domain validation gate; otherwise setting this
 *   value returns 400.
 */
export const VisibilityRequest = {
  Unlocked: "unlocked",
  Account: "account",
  Locked: "locked",
  DomainVerified: "domain_verified",
} as const;
/**
 * Controls who can view the media via this share link.
 *
 * @remarks
 *
 * - `unlocked`: anyone with the link can view the media.
 * - `account`: only signed-in members of the media's account can view.
 * - `locked`: only contacts with access to the media's folder can view.
 * - `domain_verified`: only viewers signed in with an email address at a
 *   domain verified on the media's account can view. Requires the account
 *   to be enrolled in the domain validation gate; otherwise setting this
 *   value returns 400.
 */
export type VisibilityRequest = ClosedEnum<typeof VisibilityRequest>;

/**
 * Fields that can be set when updating a share link.
 */
export type PutMediasMediaIdShareLinkRequestBody = {
  /**
   * Controls who can view the media via this share link.
   *
   * @remarks
   *
   * - `unlocked`: anyone with the link can view the media.
   * - `account`: only signed-in members of the media's account can view.
   * - `locked`: only contacts with access to the media's folder can view.
   * - `domain_verified`: only viewers signed in with an email address at a
   *   domain verified on the media's account can view. Requires the account
   *   to be enrolled in the domain validation gate; otherwise setting this
   *   value returns 400.
   */
  visibility: VisibilityRequest;
};

export type PutMediasMediaIdShareLinkRequest = {
  /**
   * The hashed ID of the media.
   */
  mediaId: string;
  requestBody: PutMediasMediaIdShareLinkRequestBody;
};

/**
 * A machine-readable identifier for the specific authorization failure.
 */
export const PutMediasMediaIdShareLinkCode = {
  UnauthorizedCredentials: "unauthorized_credentials",
  AccountInactive: "account_inactive",
  UnauthorizedScope: "unauthorized_scope",
  UnauthorizedParams: "unauthorized_params",
} as const;
/**
 * A machine-readable identifier for the specific authorization failure.
 */
export type PutMediasMediaIdShareLinkCode = ClosedEnum<
  typeof PutMediasMediaIdShareLinkCode
>;

/**
 * Controls who can view the media via this share link.
 *
 * @remarks
 *
 * - `unlocked`: anyone with the link can view the media.
 * - `account`: only signed-in members of the media's account can view.
 * - `locked`: only contacts with access to the media's folder can view.
 * - `domain_verified`: only viewers signed in with an email address at a
 *   domain verified on the media's account can view. Requires the account
 *   to be enrolled in the domain validation gate; otherwise setting this
 *   value returns 400.
 */
export const PutMediasMediaIdShareLinkVisibilityResponse = {
  Unlocked: "unlocked",
  Account: "account",
  Locked: "locked",
  DomainVerified: "domain_verified",
} as const;
/**
 * Controls who can view the media via this share link.
 *
 * @remarks
 *
 * - `unlocked`: anyone with the link can view the media.
 * - `account`: only signed-in members of the media's account can view.
 * - `locked`: only contacts with access to the media's folder can view.
 * - `domain_verified`: only viewers signed in with an email address at a
 *   domain verified on the media's account can view. Requires the account
 *   to be enrolled in the domain validation gate; otherwise setting this
 *   value returns 400.
 */
export type PutMediasMediaIdShareLinkVisibilityResponse = ClosedEnum<
  typeof PutMediasMediaIdShareLinkVisibilityResponse
>;

/**
 * A shareable link for a single media. Each media can have at most one
 *
 * @remarks
 * share link, and the link's `visibility` controls who can view the
 * media when visiting the link.
 */
export type PutMediasMediaIdShareLinkResponse = {
  /**
   * The hashed ID of the share link. This appears in the link's URL.
   */
  id: string;
  /**
   * The hashed ID of the media this share link points to.
   */
  mediaId: string;
  /**
   * The full URL viewers can use to view the media via this share link.
   */
  url: string;
  /**
   * Controls who can view the media via this share link.
   *
   * @remarks
   *
   * - `unlocked`: anyone with the link can view the media.
   * - `account`: only signed-in members of the media's account can view.
   * - `locked`: only contacts with access to the media's folder can view.
   * - `domain_verified`: only viewers signed in with an email address at a
   *   domain verified on the media's account can view. Requires the account
   *   to be enrolled in the domain validation gate; otherwise setting this
   *   value returns 400.
   */
  visibility: PutMediasMediaIdShareLinkVisibilityResponse;
  /**
   * When the share link was created.
   */
  createdAt: Date;
  /**
   * When the share link was last updated.
   */
  updatedAt: Date;
};

/** @internal */
export const VisibilityRequest$outboundSchema: z.ZodNativeEnum<
  typeof VisibilityRequest
> = z.nativeEnum(VisibilityRequest);

/** @internal */
export type PutMediasMediaIdShareLinkRequestBody$Outbound = {
  visibility: string;
};

/** @internal */
export const PutMediasMediaIdShareLinkRequestBody$outboundSchema: z.ZodType<
  PutMediasMediaIdShareLinkRequestBody$Outbound,
  z.ZodTypeDef,
  PutMediasMediaIdShareLinkRequestBody
> = z.object({
  visibility: VisibilityRequest$outboundSchema,
});

export function putMediasMediaIdShareLinkRequestBodyToJSON(
  putMediasMediaIdShareLinkRequestBody: PutMediasMediaIdShareLinkRequestBody,
): string {
  return JSON.stringify(
    PutMediasMediaIdShareLinkRequestBody$outboundSchema.parse(
      putMediasMediaIdShareLinkRequestBody,
    ),
  );
}

/** @internal */
export type PutMediasMediaIdShareLinkRequest$Outbound = {
  mediaId: string;
  RequestBody: PutMediasMediaIdShareLinkRequestBody$Outbound;
};

/** @internal */
export const PutMediasMediaIdShareLinkRequest$outboundSchema: z.ZodType<
  PutMediasMediaIdShareLinkRequest$Outbound,
  z.ZodTypeDef,
  PutMediasMediaIdShareLinkRequest
> = z.object({
  mediaId: z.string(),
  requestBody: z.lazy(() =>
    PutMediasMediaIdShareLinkRequestBody$outboundSchema
  ),
}).transform((v) => {
  return remap$(v, {
    requestBody: "RequestBody",
  });
});

export function putMediasMediaIdShareLinkRequestToJSON(
  putMediasMediaIdShareLinkRequest: PutMediasMediaIdShareLinkRequest,
): string {
  return JSON.stringify(
    PutMediasMediaIdShareLinkRequest$outboundSchema.parse(
      putMediasMediaIdShareLinkRequest,
    ),
  );
}

/** @internal */
export const PutMediasMediaIdShareLinkCode$inboundSchema: z.ZodNativeEnum<
  typeof PutMediasMediaIdShareLinkCode
> = z.nativeEnum(PutMediasMediaIdShareLinkCode);

/** @internal */
export const PutMediasMediaIdShareLinkVisibilityResponse$inboundSchema:
  z.ZodNativeEnum<typeof PutMediasMediaIdShareLinkVisibilityResponse> = z
    .nativeEnum(PutMediasMediaIdShareLinkVisibilityResponse);

/** @internal */
export const PutMediasMediaIdShareLinkResponse$inboundSchema: z.ZodType<
  PutMediasMediaIdShareLinkResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  media_id: z.string(),
  url: z.string(),
  visibility: PutMediasMediaIdShareLinkVisibilityResponse$inboundSchema,
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  updated_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "media_id": "mediaId",
    "created_at": "createdAt",
    "updated_at": "updatedAt",
  });
});

export function putMediasMediaIdShareLinkResponseFromJSON(
  jsonString: string,
): SafeParseResult<PutMediasMediaIdShareLinkResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PutMediasMediaIdShareLinkResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PutMediasMediaIdShareLinkResponse' from JSON`,
  );
}
