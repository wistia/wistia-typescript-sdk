/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
 *
 * @remarks
 * values is ignored if `cursor[before]` or `cursor[after]` are set.
 */
export const GetFoldersEnabled = {
  Zero: 0,
  One: 1,
} as const;
/**
 * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
 *
 * @remarks
 * values is ignored if `cursor[before]` or `cursor[after]` are set.
 */
export type GetFoldersEnabled = ClosedEnum<typeof GetFoldersEnabled>;

/**
 * If `cursor[enabled]` is set to 1 then cursor pagination is enabled and the
 *
 * @remarks
 * first set of records are fetched up to the `per_page`. Cursor
 * pagination will also be turned on if `cursor[before]` or `cursor[after]`
 * are set. Records returned will have a `cursor` property set which can be used to fetch more records in the same `sort_by` ordering.
 * The cursor value of the last record can be used to fetch records after the current result set and
 * the cursor of the first record can be used to fetch records before the result set.
 *
 * NOTE: a cursor value is only valid if the `sort_by` value hasn't changed from the
 * last fetch. For example, you cannot fetch using `sort_by` id and then pass that
 * cursor value to a `sort_by` name.
 */
export type GetFoldersCursor = {
  /**
   * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
   *
   * @remarks
   * values is ignored if `cursor[before]` or `cursor[after]` are set.
   */
  enabled?: GetFoldersEnabled | undefined;
  /**
   * If `cursor[before]` is set then cursor pagination is enabled and all records
   *
   * @remarks
   * before the cursor up to the `per_page` are returned. This feature is useful for
   * fetching "new records", for example, in a "pull to refersh" feature when showing records in a descending
   * order.
   */
  before?: string | undefined;
  /**
   * If `cursor[after]` is set then cursor pagination is enabled and all records
   *
   * @remarks
   * after the cursor up to the `per_page` are returned.
   */
  after?: string | undefined;
};

/**
 * Ordering. When using cursor pagination (see cursor param),
 *
 * @remarks
 * only `id`, `updated` and `created` are supported. All other sort_by options
 * require offset pagination.
 */
export const GetFoldersSortBy = {
  Name: "name",
  Created: "created",
  Updated: "updated",
  MediaCount: "mediaCount",
  Id: "id",
} as const;
/**
 * Ordering. When using cursor pagination (see cursor param),
 *
 * @remarks
 * only `id`, `updated` and `created` are supported. All other sort_by options
 * require offset pagination.
 */
export type GetFoldersSortBy = ClosedEnum<typeof GetFoldersSortBy>;

/**
 * Ordering Sort Direction (0 = desc, 1 = asc; default is 1)
 */
export const GetFoldersSortDirection = {
  Zero: 0,
  One: 1,
} as const;
/**
 * Ordering Sort Direction (0 = desc, 1 = asc; default is 1)
 */
export type GetFoldersSortDirection = ClosedEnum<
  typeof GetFoldersSortDirection
>;

export type GetFoldersRequest = {
  /**
   * The page number to retrieve. This cannot be combined with `cursor`,
   *
   * @remarks
   * pagination.
   */
  page?: number | undefined;
  /**
   * The number of medias per page. Use this for both offset pagination and cursor pagination.
   */
  perPage?: number | undefined;
  /**
   * If `cursor[enabled]` is set to 1 then cursor pagination is enabled and the
   *
   * @remarks
   * first set of records are fetched up to the `per_page`. Cursor
   * pagination will also be turned on if `cursor[before]` or `cursor[after]`
   * are set. Records returned will have a `cursor` property set which can be used to fetch more records in the same `sort_by` ordering.
   * The cursor value of the last record can be used to fetch records after the current result set and
   * the cursor of the first record can be used to fetch records before the result set.
   *
   * NOTE: a cursor value is only valid if the `sort_by` value hasn't changed from the
   * last fetch. For example, you cannot fetch using `sort_by` id and then pass that
   * cursor value to a `sort_by` name.
   */
  cursor?: GetFoldersCursor | undefined;
  /**
   * Ordering. When using cursor pagination (see cursor param),
   *
   * @remarks
   * only `id`, `updated` and `created` are supported. All other sort_by options
   * require offset pagination.
   */
  sortBy?: GetFoldersSortBy | undefined;
  /**
   * Ordering Sort Direction (0 = desc, 1 = asc; default is 1)
   */
  sortDirection?: GetFoldersSortDirection | undefined;
  /**
   * A collection of hashed ids belonging to folders to fetch
   */
  hashedIds?: Array<string> | undefined;
};

/**
 * A machine-readable identifier for the specific authorization failure.
 */
export const GetFoldersCode = {
  UnauthorizedCredentials: "unauthorized_credentials",
  AccountInactive: "account_inactive",
  UnauthorizedScope: "unauthorized_scope",
  UnauthorizedParams: "unauthorized_params",
} as const;
/**
 * A machine-readable identifier for the specific authorization failure.
 */
export type GetFoldersCode = ClosedEnum<typeof GetFoldersCode>;

/**
 * A link to where you can fetch the medias for this folder.
 */
export type GetFoldersMedias = {
  /**
   * A URL for fetching all child records of the parent record.
   */
  url?: string | undefined;
};

/**
 * Indicates the folder's access scope, relative to the requesting user. One of:
 *
 * @remarks
 * - `library`: a library the requester owns. Libraries can still be shared with specific contacts or contact groups; the only restriction is that they cannot be shared with the whole account.
 * - `shared`: a folder the requester has access to via a Contact or ContactGroup sharing — this includes both shared folders and another contact's library that the requester has been granted access to.
 * - `account`: a folder shared with the whole account (everyone in the company can see it).
 */
export const GetFoldersKind = {
  Library: "library",
  Shared: "shared",
  Account: "account",
} as const;
/**
 * Indicates the folder's access scope, relative to the requesting user. One of:
 *
 * @remarks
 * - `library`: a library the requester owns. Libraries can still be shared with specific contacts or contact groups; the only restriction is that they cannot be shared with the whole account.
 * - `shared`: a folder the requester has access to via a Contact or ContactGroup sharing — this includes both shared folders and another contact's library that the requester has been granted access to.
 * - `account`: a folder shared with the whole account (everyone in the company can see it).
 */
export type GetFoldersKind = ClosedEnum<typeof GetFoldersKind>;

/**
 * A folder (previously called a project) is a container in which to organize media into. It can be
 *
 * @remarks
 * used to set permissions that apply to all the media in the folder as well as
 * organizing media into subfolders (previously called media groups).
 */
export type GetFoldersResponse = {
  /**
   * A unique numeric identifier for the folder within the system.
   */
  id: number;
  /**
   * The folder’s display name.
   */
  name: string;
  /**
   * The folder’s description.
   */
  description?: string | null | undefined;
  /**
   * The number of different medias that have been uploaded to the folder.
   */
  mediaCount: number;
  /**
   * A link to where you can fetch the medias for this folder.
   */
  medias: GetFoldersMedias;
  /**
   * The date that the folder was originally created.
   */
  created: Date;
  /**
   * The date that the folder was last updated.
   */
  updated: Date;
  /**
   * A private hashed id, uniquely identifying the folder within the system.
   */
  hashedId: string;
  /**
   * A boolean indicating whether the folder is available for public (anonymous) viewing.
   */
  public: boolean;
  /**
   * If the folder is public, this field contains a string representing the ID used for referencing the folder in public URLs.
   */
  publicId: string | null;
  anonymousCanUpload?: boolean | undefined;
  anonymousCanDownload?: boolean | undefined;
  /**
   * Indicates the folder's access scope, relative to the requesting user. One of:
   *
   * @remarks
   * - `library`: a library the requester owns. Libraries can still be shared with specific contacts or contact groups; the only restriction is that they cannot be shared with the whole account.
   * - `shared`: a folder the requester has access to via a Contact or ContactGroup sharing — this includes both shared folders and another contact's library that the requester has been granted access to.
   * - `account`: a folder shared with the whole account (everyone in the company can see it).
   */
  kind: GetFoldersKind;
  /**
   * A cursor for stable pagination based on current `sort_by` order. You can pass this to `cursor[before]` or `cursor[after]` as a parameter to fetch the records before or after this record in the same sort order. This is only populated if records were fetched with `cursor[enabled]`, or `cursor[before]` or `cursor[after]`.
   */
  cursor?: string | null | undefined;
};

/** @internal */
export const GetFoldersEnabled$outboundSchema: z.ZodNativeEnum<
  typeof GetFoldersEnabled
> = z.nativeEnum(GetFoldersEnabled);

/** @internal */
export type GetFoldersCursor$Outbound = {
  enabled?: number | undefined;
  before?: string | undefined;
  after?: string | undefined;
};

/** @internal */
export const GetFoldersCursor$outboundSchema: z.ZodType<
  GetFoldersCursor$Outbound,
  z.ZodTypeDef,
  GetFoldersCursor
> = z.object({
  enabled: GetFoldersEnabled$outboundSchema.optional(),
  before: z.string().optional(),
  after: z.string().optional(),
});

export function getFoldersCursorToJSON(
  getFoldersCursor: GetFoldersCursor,
): string {
  return JSON.stringify(
    GetFoldersCursor$outboundSchema.parse(getFoldersCursor),
  );
}

/** @internal */
export const GetFoldersSortBy$outboundSchema: z.ZodNativeEnum<
  typeof GetFoldersSortBy
> = z.nativeEnum(GetFoldersSortBy);

/** @internal */
export const GetFoldersSortDirection$outboundSchema: z.ZodNativeEnum<
  typeof GetFoldersSortDirection
> = z.nativeEnum(GetFoldersSortDirection);

/** @internal */
export type GetFoldersRequest$Outbound = {
  page?: number | undefined;
  per_page?: number | undefined;
  cursor?: GetFoldersCursor$Outbound | undefined;
  sort_by?: string | undefined;
  sort_direction?: number | undefined;
  "hashed_ids[]"?: Array<string> | undefined;
};

/** @internal */
export const GetFoldersRequest$outboundSchema: z.ZodType<
  GetFoldersRequest$Outbound,
  z.ZodTypeDef,
  GetFoldersRequest
> = z.object({
  page: z.number().int().optional(),
  perPage: z.number().int().optional(),
  cursor: z.lazy(() => GetFoldersCursor$outboundSchema).optional(),
  sortBy: GetFoldersSortBy$outboundSchema.optional(),
  sortDirection: GetFoldersSortDirection$outboundSchema.optional(),
  hashedIds: z.array(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    perPage: "per_page",
    sortBy: "sort_by",
    sortDirection: "sort_direction",
    hashedIds: "hashed_ids[]",
  });
});

export function getFoldersRequestToJSON(
  getFoldersRequest: GetFoldersRequest,
): string {
  return JSON.stringify(
    GetFoldersRequest$outboundSchema.parse(getFoldersRequest),
  );
}

/** @internal */
export const GetFoldersCode$inboundSchema: z.ZodNativeEnum<
  typeof GetFoldersCode
> = z.nativeEnum(GetFoldersCode);

/** @internal */
export const GetFoldersMedias$inboundSchema: z.ZodType<
  GetFoldersMedias,
  z.ZodTypeDef,
  unknown
> = z.object({
  url: z.string().optional(),
});

export function getFoldersMediasFromJSON(
  jsonString: string,
): SafeParseResult<GetFoldersMedias, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFoldersMedias$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFoldersMedias' from JSON`,
  );
}

/** @internal */
export const GetFoldersKind$inboundSchema: z.ZodNativeEnum<
  typeof GetFoldersKind
> = z.nativeEnum(GetFoldersKind);

/** @internal */
export const GetFoldersResponse$inboundSchema: z.ZodType<
  GetFoldersResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.number().int(),
  name: z.string(),
  description: z.nullable(z.string()).optional(),
  media_count: z.number().int(),
  medias: z.lazy(() => GetFoldersMedias$inboundSchema),
  created: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  updated: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  hashed_id: z.string(),
  public: z.boolean(),
  public_id: z.nullable(z.string()),
  anonymous_can_upload: z.boolean().optional(),
  anonymous_can_download: z.boolean().optional(),
  kind: GetFoldersKind$inboundSchema,
  cursor: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "media_count": "mediaCount",
    "hashed_id": "hashedId",
    "public_id": "publicId",
    "anonymous_can_upload": "anonymousCanUpload",
    "anonymous_can_download": "anonymousCanDownload",
  });
});

export function getFoldersResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetFoldersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFoldersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFoldersResponse' from JSON`,
  );
}
