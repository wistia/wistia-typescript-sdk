/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Field to order by. When omitted, results are ordered most-recently-deleted first.
 */
export const GetDeletedMediaSortBy = {
  Id: "id",
  Deleted: "deleted",
} as const;
/**
 * Field to order by. When omitted, results are ordered most-recently-deleted first.
 */
export type GetDeletedMediaSortBy = ClosedEnum<typeof GetDeletedMediaSortBy>;

/**
 * Direction to order by. (0 = desc, 1 = asc; default is 1)
 */
export const GetDeletedMediaSortDirection = {
  Zero: 0,
  One: 1,
} as const;
/**
 * Direction to order by. (0 = desc, 1 = asc; default is 1)
 */
export type GetDeletedMediaSortDirection = ClosedEnum<
  typeof GetDeletedMediaSortDirection
>;

/**
 * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
 *
 * @remarks
 * values is ignored if `cursor[before]` or `cursor[after]` are set.
 */
export const GetDeletedMediaEnabled = {
  Zero: 0,
  One: 1,
} as const;
/**
 * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
 *
 * @remarks
 * values is ignored if `cursor[before]` or `cursor[after]` are set.
 */
export type GetDeletedMediaEnabled = ClosedEnum<typeof GetDeletedMediaEnabled>;

/**
 * If `cursor[enabled]` is set to 1 then cursor pagination is enabled and the
 *
 * @remarks
 * first set of records are fetched up to the `per_page`. Cursor
 * pagination will also be turned on if `cursor[before]` or `cursor[after]`
 * are set. Records returned will have a `cursor` property set which can be used to fetch more records in the same `sort_by` ordering.
 * The cursor value of the last record can be used to fetch records after the current result set and
 * the cursor of the first record can be used to fetch records before the result set.
 *
 * NOTE: a cursor value is only valid if the `sort_by` value hasn't changed from the
 * last fetch. For example, you cannot fetch using `sort_by` id and then pass that
 * cursor value to a `sort_by` name.
 */
export type GetDeletedMediaCursor = {
  /**
   * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
   *
   * @remarks
   * values is ignored if `cursor[before]` or `cursor[after]` are set.
   */
  enabled?: GetDeletedMediaEnabled | undefined;
  /**
   * If `cursor[before]` is set then cursor pagination is enabled and all records
   *
   * @remarks
   * before the cursor up to the `per_page` are returned. This feature is useful for
   * fetching "new records", for example, in a "pull to refersh" feature when showing records in a descending
   * order.
   */
  before?: string | undefined;
  /**
   * If `cursor[after]` is set then cursor pagination is enabled and all records
   *
   * @remarks
   * after the cursor up to the `per_page` are returned.
   */
  after?: string | undefined;
};

export type GetDeletedMediaRequest = {
  /**
   * Restrict the results to the deleted media with these hashed IDs.
   */
  hashedIds?: Array<string> | undefined;
  /**
   * Field to order by. When omitted, results are ordered most-recently-deleted first.
   */
  sortBy?: GetDeletedMediaSortBy | undefined;
  /**
   * Direction to order by. (0 = desc, 1 = asc; default is 1)
   */
  sortDirection?: GetDeletedMediaSortDirection | undefined;
  /**
   * The page number to retrieve. This cannot be combined with `cursor`,
   *
   * @remarks
   * pagination.
   */
  page?: number | undefined;
  /**
   * The number of medias per page. Use this for both offset pagination and cursor pagination.
   */
  perPage?: number | undefined;
  /**
   * If `cursor[enabled]` is set to 1 then cursor pagination is enabled and the
   *
   * @remarks
   * first set of records are fetched up to the `per_page`. Cursor
   * pagination will also be turned on if `cursor[before]` or `cursor[after]`
   * are set. Records returned will have a `cursor` property set which can be used to fetch more records in the same `sort_by` ordering.
   * The cursor value of the last record can be used to fetch records after the current result set and
   * the cursor of the first record can be used to fetch records before the result set.
   *
   * NOTE: a cursor value is only valid if the `sort_by` value hasn't changed from the
   * last fetch. For example, you cannot fetch using `sort_by` id and then pass that
   * cursor value to a `sort_by` name.
   */
  cursor?: GetDeletedMediaCursor | undefined;
};

/**
 * A machine-readable identifier for the specific authorization failure.
 */
export const GetDeletedMediaCode = {
  UnauthorizedCredentials: "unauthorized_credentials",
  AccountInactive: "account_inactive",
  UnauthorizedScope: "unauthorized_scope",
  UnauthorizedParams: "unauthorized_params",
} as const;
/**
 * A machine-readable identifier for the specific authorization failure.
 */
export type GetDeletedMediaCode = ClosedEnum<typeof GetDeletedMediaCode>;

/**
 * The folder the media will return to if restored without an explicit target. Null if the original folder was also deleted.
 */
export type GetDeletedMediaFolder = {
  hashedId?: string | undefined;
  name?: string | null | undefined;
};

export type GetDeletedMediaResponse = {
  /**
   * A unique alphanumeric identifier for this media.
   */
  hashedId?: string | undefined;
  /**
   * The media display name.
   */
  name?: string | undefined;
  /**
   * The type of media (e.g. Video, Audio).
   */
  type?: string | undefined;
  /**
   * The length of the media in seconds.
   */
  duration?: number | null | undefined;
  /**
   * When the media was deleted, in ISO 8601 format.
   */
  deletedAt?: Date | undefined;
  /**
   * The deadline after which this media is permanently purged and can no longer be restored (deleted_at plus the account's restore window).
   */
  restoreExpiresAt?: Date | undefined;
  /**
   * The folder the media will return to if restored without an explicit target. Null if the original folder was also deleted.
   */
  folder?: GetDeletedMediaFolder | null | undefined;
  /**
   * A cursor for stable pagination based on current `sort_by` order. You can pass this to `cursor[before]` or `cursor[after]` as a parameter to fetch the records before or after this record in the same sort order. This is only populated if records were fetched with `cursor[enabled]`, or `cursor[before]` or `cursor[after]`.
   */
  cursor?: string | null | undefined;
};

/** @internal */
export const GetDeletedMediaSortBy$outboundSchema: z.ZodNativeEnum<
  typeof GetDeletedMediaSortBy
> = z.nativeEnum(GetDeletedMediaSortBy);

/** @internal */
export const GetDeletedMediaSortDirection$outboundSchema: z.ZodNativeEnum<
  typeof GetDeletedMediaSortDirection
> = z.nativeEnum(GetDeletedMediaSortDirection);

/** @internal */
export const GetDeletedMediaEnabled$outboundSchema: z.ZodNativeEnum<
  typeof GetDeletedMediaEnabled
> = z.nativeEnum(GetDeletedMediaEnabled);

/** @internal */
export type GetDeletedMediaCursor$Outbound = {
  enabled?: number | undefined;
  before?: string | undefined;
  after?: string | undefined;
};

/** @internal */
export const GetDeletedMediaCursor$outboundSchema: z.ZodType<
  GetDeletedMediaCursor$Outbound,
  z.ZodTypeDef,
  GetDeletedMediaCursor
> = z.object({
  enabled: GetDeletedMediaEnabled$outboundSchema.optional(),
  before: z.string().optional(),
  after: z.string().optional(),
});

export function getDeletedMediaCursorToJSON(
  getDeletedMediaCursor: GetDeletedMediaCursor,
): string {
  return JSON.stringify(
    GetDeletedMediaCursor$outboundSchema.parse(getDeletedMediaCursor),
  );
}

/** @internal */
export type GetDeletedMediaRequest$Outbound = {
  "hashed_ids[]"?: Array<string> | undefined;
  sort_by?: string | undefined;
  sort_direction?: number | undefined;
  page?: number | undefined;
  per_page?: number | undefined;
  cursor?: GetDeletedMediaCursor$Outbound | undefined;
};

/** @internal */
export const GetDeletedMediaRequest$outboundSchema: z.ZodType<
  GetDeletedMediaRequest$Outbound,
  z.ZodTypeDef,
  GetDeletedMediaRequest
> = z.object({
  hashedIds: z.array(z.string()).optional(),
  sortBy: GetDeletedMediaSortBy$outboundSchema.optional(),
  sortDirection: GetDeletedMediaSortDirection$outboundSchema.optional(),
  page: z.number().int().optional(),
  perPage: z.number().int().optional(),
  cursor: z.lazy(() => GetDeletedMediaCursor$outboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    hashedIds: "hashed_ids[]",
    sortBy: "sort_by",
    sortDirection: "sort_direction",
    perPage: "per_page",
  });
});

export function getDeletedMediaRequestToJSON(
  getDeletedMediaRequest: GetDeletedMediaRequest,
): string {
  return JSON.stringify(
    GetDeletedMediaRequest$outboundSchema.parse(getDeletedMediaRequest),
  );
}

/** @internal */
export const GetDeletedMediaCode$inboundSchema: z.ZodNativeEnum<
  typeof GetDeletedMediaCode
> = z.nativeEnum(GetDeletedMediaCode);

/** @internal */
export const GetDeletedMediaFolder$inboundSchema: z.ZodType<
  GetDeletedMediaFolder,
  z.ZodTypeDef,
  unknown
> = z.object({
  hashed_id: z.string().optional(),
  name: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "hashed_id": "hashedId",
  });
});

export function getDeletedMediaFolderFromJSON(
  jsonString: string,
): SafeParseResult<GetDeletedMediaFolder, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetDeletedMediaFolder$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetDeletedMediaFolder' from JSON`,
  );
}

/** @internal */
export const GetDeletedMediaResponse$inboundSchema: z.ZodType<
  GetDeletedMediaResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  hashed_id: z.string().optional(),
  name: z.string().optional(),
  type: z.string().optional(),
  duration: z.nullable(z.number()).optional(),
  deleted_at: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  restore_expires_at: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ).optional(),
  folder: z.nullable(z.lazy(() => GetDeletedMediaFolder$inboundSchema))
    .optional(),
  cursor: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "hashed_id": "hashedId",
    "deleted_at": "deletedAt",
    "restore_expires_at": "restoreExpiresAt",
  });
});

export function getDeletedMediaResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetDeletedMediaResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetDeletedMediaResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetDeletedMediaResponse' from JSON`,
  );
}
