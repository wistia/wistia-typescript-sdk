/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
 *
 * @remarks
 * values is ignored if `cursor[before]` or `cursor[after]` are set.
 */
export const GetBrandsEnabled = {
  Zero: 0,
  One: 1,
} as const;
/**
 * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
 *
 * @remarks
 * values is ignored if `cursor[before]` or `cursor[after]` are set.
 */
export type GetBrandsEnabled = ClosedEnum<typeof GetBrandsEnabled>;

/**
 * If `cursor[enabled]` is set to 1 then cursor pagination is enabled and the
 *
 * @remarks
 * first set of records are fetched up to the `per_page`. Cursor
 * pagination will also be turned on if `cursor[before]` or `cursor[after]`
 * are set. Records returned will have a `cursor` property set which can be used to fetch more records in the same `sort_by` ordering.
 * The cursor value of the last record can be used to fetch records after the current result set and
 * the cursor of the first record can be used to fetch records before the result set.
 *
 * NOTE: a cursor value is only valid if the `sort_by` value hasn't changed from the
 * last fetch. For example, you cannot fetch using `sort_by` id and then pass that
 * cursor value to a `sort_by` name.
 */
export type GetBrandsCursor = {
  /**
   * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
   *
   * @remarks
   * values is ignored if `cursor[before]` or `cursor[after]` are set.
   */
  enabled?: GetBrandsEnabled | undefined;
  /**
   * If `cursor[before]` is set then cursor pagination is enabled and all records
   *
   * @remarks
   * before the cursor up to the `per_page` are returned. This feature is useful for
   * fetching "new records", for example, in a "pull to refersh" feature when showing records in a descending
   * order.
   */
  before?: string | undefined;
  /**
   * If `cursor[after]` is set then cursor pagination is enabled and all records
   *
   * @remarks
   * after the cursor up to the `per_page` are returned.
   */
  after?: string | undefined;
};

/**
 * Ordering. When using cursor pagination (see cursor param),
 *
 * @remarks
 * only `id`, `updated` and `created` are supported. All other sort_by options
 * require offset pagination.
 */
export const GetBrandsSortBy = {
  Name: "name",
  Created: "created",
  Updated: "updated",
  Id: "id",
} as const;
/**
 * Ordering. When using cursor pagination (see cursor param),
 *
 * @remarks
 * only `id`, `updated` and `created` are supported. All other sort_by options
 * require offset pagination.
 */
export type GetBrandsSortBy = ClosedEnum<typeof GetBrandsSortBy>;

/**
 * Ordering Sort Direction (0 = desc, 1 = asc)
 */
export const GetBrandsSortDirection = {
  Zero: 0,
  One: 1,
} as const;
/**
 * Ordering Sort Direction (0 = desc, 1 = asc)
 */
export type GetBrandsSortDirection = ClosedEnum<typeof GetBrandsSortDirection>;

export type GetBrandsRequest = {
  /**
   * The page number to retrieve. This cannot be combined with `cursor`,
   *
   * @remarks
   * pagination.
   */
  page?: number | undefined;
  /**
   * The number of medias per page. Use this for both offset pagination and cursor pagination.
   */
  perPage?: number | undefined;
  /**
   * If `cursor[enabled]` is set to 1 then cursor pagination is enabled and the
   *
   * @remarks
   * first set of records are fetched up to the `per_page`. Cursor
   * pagination will also be turned on if `cursor[before]` or `cursor[after]`
   * are set. Records returned will have a `cursor` property set which can be used to fetch more records in the same `sort_by` ordering.
   * The cursor value of the last record can be used to fetch records after the current result set and
   * the cursor of the first record can be used to fetch records before the result set.
   *
   * NOTE: a cursor value is only valid if the `sort_by` value hasn't changed from the
   * last fetch. For example, you cannot fetch using `sort_by` id and then pass that
   * cursor value to a `sort_by` name.
   */
  cursor?: GetBrandsCursor | undefined;
  /**
   * Ordering. When using cursor pagination (see cursor param),
   *
   * @remarks
   * only `id`, `updated` and `created` are supported. All other sort_by options
   * require offset pagination.
   */
  sortBy?: GetBrandsSortBy | undefined;
  /**
   * Ordering Sort Direction (0 = desc, 1 = asc)
   */
  sortDirection?: GetBrandsSortDirection | undefined;
};

/**
 * A machine-readable identifier for the specific authorization failure.
 */
export const GetBrandsCode = {
  UnauthorizedCredentials: "unauthorized_credentials",
  AccountInactive: "account_inactive",
  UnauthorizedScope: "unauthorized_scope",
  UnauthorizedParams: "unauthorized_params",
} as const;
/**
 * A machine-readable identifier for the specific authorization failure.
 */
export type GetBrandsCode = ClosedEnum<typeof GetBrandsCode>;

/**
 * The primary brand color, used for primary buttons and the player playbar. Either a hex color string or a gradient represented as an array of [hexColor, percentage] tuples. Null when unset.
 */
export type PrimaryColor = string | Array<Array<any>>;

/**
 * The brand color used for page backgrounds. Either a hex color string or a gradient represented as an array of [hexColor, percentage] tuples. Null when unset.
 */
export type PageBackgroundColor = string | Array<Array<any>>;

/**
 * Controls whether the player icon color is always white or uses an accessible contrast color when necessary.
 */
export const ContrastIcons = {
  Enabled: "enabled",
  Disabled: "disabled",
  Unset: "unset",
} as const;
/**
 * Controls whether the player icon color is always white or uses an accessible contrast color when necessary.
 */
export type ContrastIcons = ClosedEnum<typeof ContrastIcons>;

/**
 * Controls the opacity of the video player control bar and big play button.
 */
export const OpaqueControls = {
  Enabled: "enabled",
  Disabled: "disabled",
  Unset: "unset",
} as const;
/**
 * Controls the opacity of the video player control bar and big play button.
 */
export type OpaqueControls = ClosedEnum<typeof OpaqueControls>;

export type PageLogoDimensions = {
  width?: number | undefined;
  height?: number | undefined;
};

/**
 * The brand logo used for pages. Null when unset.
 */
export type PageLogo = {
  /**
   * The URL of the logo image.
   */
  url?: string | undefined;
  dimensions?: PageLogoDimensions | null | undefined;
  /**
   * The size multiplier of the logo.
   */
  size?: number | null | undefined;
};

export type PlayerLogoDimensions = {
  width?: number | undefined;
  height?: number | undefined;
};

/**
 * The brand logo used for the player. Null when unset.
 */
export type PlayerLogo = {
  /**
   * The URL of the logo image.
   */
  url?: string | undefined;
  dimensions?: PlayerLogoDimensions | null | undefined;
  /**
   * The size multiplier of the logo.
   */
  size?: number | null | undefined;
};

/**
 * A brand is a saved set of branding options — colors, fonts, logos, and
 *
 * @remarks
 * layout — that can be applied to media, projects, channels, and webinars.
 */
export type GetBrandsResponse = {
  /**
   * The brand's display name.
   */
  name?: string | undefined;
  /**
   * Whether this brand is the account-level default, applied to content that has no more specific brand.
   */
  isDefault?: boolean | undefined;
  /**
   * The date the brand was originally created.
   */
  created?: Date | undefined;
  /**
   * The date the brand was last updated.
   */
  updated?: Date | undefined;
  /**
   * The primary brand color, used for primary buttons and the player playbar. Either a hex color string or a gradient represented as an array of [hexColor, percentage] tuples. Null when unset.
   */
  primaryColor?: string | Array<Array<any>> | null | undefined;
  /**
   * The brand color used for page backgrounds. Either a hex color string or a gradient represented as an array of [hexColor, percentage] tuples. Null when unset.
   */
  pageBackgroundColor?: string | Array<Array<any>> | null | undefined;
  /**
   * The brand font family for body text. Null when unset.
   */
  bodyFontFamily?: string | null | undefined;
  /**
   * The brand font family for headlines. Null when unset.
   */
  headlineFontFamily?: string | null | undefined;
  /**
   * The brand font family for buttons. Null when unset.
   */
  buttonFontFamily?: string | null | undefined;
  /**
   * The border radius in pixels for rounded corners. Null when unset.
   */
  borderRadius?: number | null | undefined;
  /**
   * Controls whether the player icon color is always white or uses an accessible contrast color when necessary.
   */
  contrastIcons?: ContrastIcons | null | undefined;
  /**
   * Controls the opacity of the video player control bar and big play button.
   */
  opaqueControls?: OpaqueControls | null | undefined;
  /**
   * The brand logo used for pages. Null when unset.
   */
  pageLogo?: PageLogo | null | undefined;
  /**
   * The brand logo used for the player. Null when unset.
   */
  playerLogo?: PlayerLogo | null | undefined;
  /**
   * A cursor for stable pagination based on current `sort_by` order. You can pass this to `cursor[before]` or `cursor[after]` as a parameter to fetch the records before or after this record in the same sort order. This is only populated if records were fetched with `cursor[enabled]`, or `cursor[before]` or `cursor[after]`.
   */
  cursor?: string | null | undefined;
};

/** @internal */
export const GetBrandsEnabled$outboundSchema: z.ZodNativeEnum<
  typeof GetBrandsEnabled
> = z.nativeEnum(GetBrandsEnabled);

/** @internal */
export type GetBrandsCursor$Outbound = {
  enabled?: number | undefined;
  before?: string | undefined;
  after?: string | undefined;
};

/** @internal */
export const GetBrandsCursor$outboundSchema: z.ZodType<
  GetBrandsCursor$Outbound,
  z.ZodTypeDef,
  GetBrandsCursor
> = z.object({
  enabled: GetBrandsEnabled$outboundSchema.optional(),
  before: z.string().optional(),
  after: z.string().optional(),
});

export function getBrandsCursorToJSON(
  getBrandsCursor: GetBrandsCursor,
): string {
  return JSON.stringify(GetBrandsCursor$outboundSchema.parse(getBrandsCursor));
}

/** @internal */
export const GetBrandsSortBy$outboundSchema: z.ZodNativeEnum<
  typeof GetBrandsSortBy
> = z.nativeEnum(GetBrandsSortBy);

/** @internal */
export const GetBrandsSortDirection$outboundSchema: z.ZodNativeEnum<
  typeof GetBrandsSortDirection
> = z.nativeEnum(GetBrandsSortDirection);

/** @internal */
export type GetBrandsRequest$Outbound = {
  page?: number | undefined;
  per_page?: number | undefined;
  cursor?: GetBrandsCursor$Outbound | undefined;
  sort_by?: string | undefined;
  sort_direction?: number | undefined;
};

/** @internal */
export const GetBrandsRequest$outboundSchema: z.ZodType<
  GetBrandsRequest$Outbound,
  z.ZodTypeDef,
  GetBrandsRequest
> = z.object({
  page: z.number().int().optional(),
  perPage: z.number().int().optional(),
  cursor: z.lazy(() => GetBrandsCursor$outboundSchema).optional(),
  sortBy: GetBrandsSortBy$outboundSchema.optional(),
  sortDirection: GetBrandsSortDirection$outboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    perPage: "per_page",
    sortBy: "sort_by",
    sortDirection: "sort_direction",
  });
});

export function getBrandsRequestToJSON(
  getBrandsRequest: GetBrandsRequest,
): string {
  return JSON.stringify(
    GetBrandsRequest$outboundSchema.parse(getBrandsRequest),
  );
}

/** @internal */
export const GetBrandsCode$inboundSchema: z.ZodNativeEnum<
  typeof GetBrandsCode
> = z.nativeEnum(GetBrandsCode);

/** @internal */
export const PrimaryColor$inboundSchema: z.ZodType<
  PrimaryColor,
  z.ZodTypeDef,
  unknown
> = z.union([z.string(), z.array(z.array(z.any()))]);

export function primaryColorFromJSON(
  jsonString: string,
): SafeParseResult<PrimaryColor, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PrimaryColor$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PrimaryColor' from JSON`,
  );
}

/** @internal */
export const PageBackgroundColor$inboundSchema: z.ZodType<
  PageBackgroundColor,
  z.ZodTypeDef,
  unknown
> = z.union([z.string(), z.array(z.array(z.any()))]);

export function pageBackgroundColorFromJSON(
  jsonString: string,
): SafeParseResult<PageBackgroundColor, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PageBackgroundColor$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PageBackgroundColor' from JSON`,
  );
}

/** @internal */
export const ContrastIcons$inboundSchema: z.ZodNativeEnum<
  typeof ContrastIcons
> = z.nativeEnum(ContrastIcons);

/** @internal */
export const OpaqueControls$inboundSchema: z.ZodNativeEnum<
  typeof OpaqueControls
> = z.nativeEnum(OpaqueControls);

/** @internal */
export const PageLogoDimensions$inboundSchema: z.ZodType<
  PageLogoDimensions,
  z.ZodTypeDef,
  unknown
> = z.object({
  width: z.number().int().optional(),
  height: z.number().int().optional(),
});

export function pageLogoDimensionsFromJSON(
  jsonString: string,
): SafeParseResult<PageLogoDimensions, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PageLogoDimensions$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PageLogoDimensions' from JSON`,
  );
}

/** @internal */
export const PageLogo$inboundSchema: z.ZodType<
  PageLogo,
  z.ZodTypeDef,
  unknown
> = z.object({
  url: z.string().optional(),
  dimensions: z.nullable(z.lazy(() => PageLogoDimensions$inboundSchema))
    .optional(),
  size: z.nullable(z.number()).optional(),
});

export function pageLogoFromJSON(
  jsonString: string,
): SafeParseResult<PageLogo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PageLogo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PageLogo' from JSON`,
  );
}

/** @internal */
export const PlayerLogoDimensions$inboundSchema: z.ZodType<
  PlayerLogoDimensions,
  z.ZodTypeDef,
  unknown
> = z.object({
  width: z.number().int().optional(),
  height: z.number().int().optional(),
});

export function playerLogoDimensionsFromJSON(
  jsonString: string,
): SafeParseResult<PlayerLogoDimensions, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlayerLogoDimensions$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlayerLogoDimensions' from JSON`,
  );
}

/** @internal */
export const PlayerLogo$inboundSchema: z.ZodType<
  PlayerLogo,
  z.ZodTypeDef,
  unknown
> = z.object({
  url: z.string().optional(),
  dimensions: z.nullable(z.lazy(() => PlayerLogoDimensions$inboundSchema))
    .optional(),
  size: z.nullable(z.number()).optional(),
});

export function playerLogoFromJSON(
  jsonString: string,
): SafeParseResult<PlayerLogo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlayerLogo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlayerLogo' from JSON`,
  );
}

/** @internal */
export const GetBrandsResponse$inboundSchema: z.ZodType<
  GetBrandsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string().optional(),
  is_default: z.boolean().optional(),
  created: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  updated: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  primary_color: z.nullable(z.union([z.string(), z.array(z.array(z.any()))]))
    .optional(),
  page_background_color: z.nullable(
    z.union([z.string(), z.array(z.array(z.any()))]),
  ).optional(),
  body_font_family: z.nullable(z.string()).optional(),
  headline_font_family: z.nullable(z.string()).optional(),
  button_font_family: z.nullable(z.string()).optional(),
  border_radius: z.nullable(z.number().int()).optional(),
  contrast_icons: z.nullable(ContrastIcons$inboundSchema).optional(),
  opaque_controls: z.nullable(OpaqueControls$inboundSchema).optional(),
  page_logo: z.nullable(z.lazy(() => PageLogo$inboundSchema)).optional(),
  player_logo: z.nullable(z.lazy(() => PlayerLogo$inboundSchema)).optional(),
  cursor: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "is_default": "isDefault",
    "primary_color": "primaryColor",
    "page_background_color": "pageBackgroundColor",
    "body_font_family": "bodyFontFamily",
    "headline_font_family": "headlineFontFamily",
    "button_font_family": "buttonFontFamily",
    "border_radius": "borderRadius",
    "contrast_icons": "contrastIcons",
    "opaque_controls": "opaqueControls",
    "page_logo": "pageLogo",
    "player_logo": "playerLogo",
  });
});

export function getBrandsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetBrandsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetBrandsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetBrandsResponse' from JSON`,
  );
}
