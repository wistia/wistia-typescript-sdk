/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The APNs environment the token belongs to.
 */
export const EnvironmentRequest = {
  Sandbox: "sandbox",
  Production: "production",
} as const;
/**
 * The APNs environment the token belongs to.
 */
export type EnvironmentRequest = ClosedEnum<typeof EnvironmentRequest>;

export type PostDevicesRequest = {
  /**
   * The APNs device token, hex encoded.
   */
  deviceToken: string;
  /**
   * The app bundle identifier the token was issued for.
   */
  bundleId: string;
  /**
   * The APNs environment the token belongs to.
   */
  environment: EnvironmentRequest;
};

/**
 * A machine-readable identifier for the specific authorization failure.
 */
export const PostDevicesCode = {
  UnauthorizedCredentials: "unauthorized_credentials",
  AccountInactive: "account_inactive",
  UnauthorizedScope: "unauthorized_scope",
  UnauthorizedParams: "unauthorized_params",
} as const;
/**
 * A machine-readable identifier for the specific authorization failure.
 */
export type PostDevicesCode = ClosedEnum<typeof PostDevicesCode>;

/**
 * The APNs environment the token belongs to.
 */
export const PostDevicesEnvironmentResponse = {
  Sandbox: "sandbox",
  Production: "production",
} as const;
/**
 * The APNs environment the token belongs to.
 */
export type PostDevicesEnvironmentResponse = ClosedEnum<
  typeof PostDevicesEnvironmentResponse
>;

/**
 * A registered push notification device token for the Wistia desktop app.
 *
 * @remarks
 * Used to deliver activity notifications (new comments and views)
 * for media created via the desktop app.
 */
export type PostDevicesResponse = {
  /**
   * The APNs device token, hex encoded.
   */
  deviceToken: string;
  /**
   * The app bundle identifier the token was issued for.
   */
  bundleId: string;
  /**
   * The APNs environment the token belongs to.
   */
  environment: PostDevicesEnvironmentResponse;
  /**
   * The date the device was first registered.
   */
  createdAt: Date;
  /**
   * A cursor for stable pagination based on current `sort_by` order. You can pass this to `cursor[before]` or `cursor[after]` as a parameter to fetch the records before or after this record in the same sort order. This is only populated if records were fetched with `cursor[enabled]`, or `cursor[before]` or `cursor[after]`.
   */
  cursor?: string | null | undefined;
};

/** @internal */
export const EnvironmentRequest$outboundSchema: z.ZodNativeEnum<
  typeof EnvironmentRequest
> = z.nativeEnum(EnvironmentRequest);

/** @internal */
export type PostDevicesRequest$Outbound = {
  device_token: string;
  bundle_id: string;
  environment: string;
};

/** @internal */
export const PostDevicesRequest$outboundSchema: z.ZodType<
  PostDevicesRequest$Outbound,
  z.ZodTypeDef,
  PostDevicesRequest
> = z.object({
  deviceToken: z.string(),
  bundleId: z.string(),
  environment: EnvironmentRequest$outboundSchema,
}).transform((v) => {
  return remap$(v, {
    deviceToken: "device_token",
    bundleId: "bundle_id",
  });
});

export function postDevicesRequestToJSON(
  postDevicesRequest: PostDevicesRequest,
): string {
  return JSON.stringify(
    PostDevicesRequest$outboundSchema.parse(postDevicesRequest),
  );
}

/** @internal */
export const PostDevicesCode$inboundSchema: z.ZodNativeEnum<
  typeof PostDevicesCode
> = z.nativeEnum(PostDevicesCode);

/** @internal */
export const PostDevicesEnvironmentResponse$inboundSchema: z.ZodNativeEnum<
  typeof PostDevicesEnvironmentResponse
> = z.nativeEnum(PostDevicesEnvironmentResponse);

/** @internal */
export const PostDevicesResponse$inboundSchema: z.ZodType<
  PostDevicesResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  device_token: z.string(),
  bundle_id: z.string(),
  environment: PostDevicesEnvironmentResponse$inboundSchema,
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  cursor: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "device_token": "deviceToken",
    "bundle_id": "bundleId",
    "created_at": "createdAt",
  });
});

export function postDevicesResponseFromJSON(
  jsonString: string,
): SafeParseResult<PostDevicesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PostDevicesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PostDevicesResponse' from JSON`,
  );
}
