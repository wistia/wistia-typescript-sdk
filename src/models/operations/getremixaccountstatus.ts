/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Remix account status.
 */
export type GetRemixAccountStatusResponse = {
  /**
   * Maximum number of remix credits per month.
   */
  monthlyLimit?: number | undefined;
  /**
   * Number of remix credits used this month.
   */
  monthlyUsage?: number | undefined;
  /**
   * Number of remix credits remaining this month.
   */
  remaining?: number | undefined;
  /**
   * Whether the account can create a new remix.
   */
  canCreateRemix?: boolean | undefined;
};

/** @internal */
export const GetRemixAccountStatusResponse$inboundSchema: z.ZodType<
  GetRemixAccountStatusResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  monthly_limit: z.number().int().optional(),
  monthly_usage: z.number().int().optional(),
  remaining: z.number().int().optional(),
  can_create_remix: z.boolean().optional(),
}).transform((v) => {
  return remap$(v, {
    "monthly_limit": "monthlyLimit",
    "monthly_usage": "monthlyUsage",
    "can_create_remix": "canCreateRemix",
  });
});

export function getRemixAccountStatusResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetRemixAccountStatusResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetRemixAccountStatusResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetRemixAccountStatusResponse' from JSON`,
  );
}
