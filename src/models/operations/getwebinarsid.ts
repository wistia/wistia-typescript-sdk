/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetWebinarsIdRequest = {
  /**
   * The hashed ID of the webinar
   */
  id: string;
};

/**
 * A machine-readable identifier for the specific authorization failure.
 */
export const GetWebinarsIdCode = {
  UnauthorizedCredentials: "unauthorized_credentials",
  AccountInactive: "account_inactive",
  UnauthorizedScope: "unauthorized_scope",
  UnauthorizedParams: "unauthorized_params",
} as const;
/**
 * A machine-readable identifier for the specific authorization failure.
 */
export type GetWebinarsIdCode = ClosedEnum<typeof GetWebinarsIdCode>;

/**
 * The current lifecycle status of the webinar. This is a read-only, system-managed field that Wistia updates as the event moves through its lifecycle; it cannot be set or changed via the API.
 */
export const GetWebinarsIdLifecycleStatus = {
  Pending: "pending",
  Ready: "ready",
  Starting: "starting",
  Started: "started",
  Ended: "ended",
  VodReady: "vod_ready",
  Failed: "failed",
} as const;
/**
 * The current lifecycle status of the webinar. This is a read-only, system-managed field that Wistia updates as the event moves through its lifecycle; it cannot be set or changed via the API.
 */
export type GetWebinarsIdLifecycleStatus = ClosedEnum<
  typeof GetWebinarsIdLifecycleStatus
>;

export type GetWebinarsIdFolder = {
  /**
   * A unique alphanumeric identifier for the record.
   */
  id: string;
  /**
   * A URL for fetching all the records of the given record type. You can pass hashed_ids as a param with multiple values
   *
   * @remarks
   * to do a batch fetch for this records type.
   */
  indexUrl: string;
  /**
   * A URL that can be used to fetch this record.
   */
  url: string;
};

/**
 * A webinar is an event which allows you to stream a video
 *
 * @remarks
 * to multiple participants. See our [Webinars Guide](https://support.wistia.com/en/articles/8288501-getting-started-with-webinars)
 * for more info.
 */
export type GetWebinarsIdResponse = {
  /**
   * The hashed ID of the webinar
   */
  id: string;
  /**
   * The title of the webinar
   */
  title: string;
  /**
   * The description of the webinar
   */
  description?: string | null | undefined;
  /**
   * The scheduled start time in W3C format with timezone
   */
  scheduledFor?: Date | null | undefined;
  /**
   * Duration of the webinar in minutes
   */
  eventDuration?: number | null | undefined;
  /**
   * The IANA time zone identifier the webinar is scheduled in
   */
  timeZone: string;
  /**
   * The current lifecycle status of the webinar. This is a read-only, system-managed field that Wistia updates as the event moves through its lifecycle; it cannot be set or changed via the API.
   */
  lifecycleStatus: GetWebinarsIdLifecycleStatus;
  /**
   * Registration status of the event
   */
  registrationStatus: string;
  /**
   * When the event was created (UTC)
   */
  createdAt: Date;
  /**
   * When the event was last updated (UTC)
   */
  updatedAt: Date;
  /**
   * Link for the audience to join the event
   */
  audienceLink: string;
  /**
   * Link for the host to manage the event
   */
  hostLink: string;
  /**
   * Link for panelists to join the event
   */
  panelistLink: string;
  /**
   * URL of the webinar's custom thumbnail image, or null if no custom thumbnail has been set
   */
  thumbnailUrl?: string | null | undefined;
  /**
   * The folder (project) this webinar belongs to
   */
  folder?: GetWebinarsIdFolder | null | undefined;
  /**
   * A cursor for stable pagination based on current `sort_by` order. You can pass this to `cursor[before]` or `cursor[after]` as a parameter to fetch the records before or after this record in the same sort order. This is only populated if records were fetched with `cursor[enabled]`, or `cursor[before]` or `cursor[after]`.
   */
  cursor?: string | null | undefined;
};

/** @internal */
export type GetWebinarsIdRequest$Outbound = {
  id: string;
};

/** @internal */
export const GetWebinarsIdRequest$outboundSchema: z.ZodType<
  GetWebinarsIdRequest$Outbound,
  z.ZodTypeDef,
  GetWebinarsIdRequest
> = z.object({
  id: z.string(),
});

export function getWebinarsIdRequestToJSON(
  getWebinarsIdRequest: GetWebinarsIdRequest,
): string {
  return JSON.stringify(
    GetWebinarsIdRequest$outboundSchema.parse(getWebinarsIdRequest),
  );
}

/** @internal */
export const GetWebinarsIdCode$inboundSchema: z.ZodNativeEnum<
  typeof GetWebinarsIdCode
> = z.nativeEnum(GetWebinarsIdCode);

/** @internal */
export const GetWebinarsIdLifecycleStatus$inboundSchema: z.ZodNativeEnum<
  typeof GetWebinarsIdLifecycleStatus
> = z.nativeEnum(GetWebinarsIdLifecycleStatus);

/** @internal */
export const GetWebinarsIdFolder$inboundSchema: z.ZodType<
  GetWebinarsIdFolder,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  index_url: z.string(),
  url: z.string(),
}).transform((v) => {
  return remap$(v, {
    "index_url": "indexUrl",
  });
});

export function getWebinarsIdFolderFromJSON(
  jsonString: string,
): SafeParseResult<GetWebinarsIdFolder, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetWebinarsIdFolder$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetWebinarsIdFolder' from JSON`,
  );
}

/** @internal */
export const GetWebinarsIdResponse$inboundSchema: z.ZodType<
  GetWebinarsIdResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  title: z.string(),
  description: z.nullable(z.string()).optional(),
  scheduled_for: z.nullable(
    z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ).optional(),
  event_duration: z.nullable(z.number().int()).optional(),
  time_zone: z.string(),
  lifecycle_status: GetWebinarsIdLifecycleStatus$inboundSchema,
  registration_status: z.string(),
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  updated_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  audience_link: z.string(),
  host_link: z.string(),
  panelist_link: z.string(),
  thumbnail_url: z.nullable(z.string()).optional(),
  folder: z.nullable(z.lazy(() => GetWebinarsIdFolder$inboundSchema))
    .optional(),
  cursor: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "scheduled_for": "scheduledFor",
    "event_duration": "eventDuration",
    "time_zone": "timeZone",
    "lifecycle_status": "lifecycleStatus",
    "registration_status": "registrationStatus",
    "created_at": "createdAt",
    "updated_at": "updatedAt",
    "audience_link": "audienceLink",
    "host_link": "hostLink",
    "panelist_link": "panelistLink",
    "thumbnail_url": "thumbnailUrl",
  });
});

export function getWebinarsIdResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetWebinarsIdResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetWebinarsIdResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetWebinarsIdResponse' from JSON`,
  );
}
