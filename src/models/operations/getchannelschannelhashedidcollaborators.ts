/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
 *
 * @remarks
 * values is ignored if `cursor[before]` or `cursor[after]` are set.
 */
export const GetChannelsChannelHashedIdCollaboratorsEnabled = {
  Zero: 0,
  One: 1,
} as const;
/**
 * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
 *
 * @remarks
 * values is ignored if `cursor[before]` or `cursor[after]` are set.
 */
export type GetChannelsChannelHashedIdCollaboratorsEnabled = ClosedEnum<
  typeof GetChannelsChannelHashedIdCollaboratorsEnabled
>;

/**
 * If `cursor[enabled]` is set to 1 then cursor pagination is enabled and the
 *
 * @remarks
 * first set of records are fetched up to the `per_page`. Cursor
 * pagination will also be turned on if `cursor[before]` or `cursor[after]`
 * are set. Records returned will have a `cursor` property set which can be used to fetch more records in the same `sort_by` ordering.
 * The cursor value of the last record can be used to fetch records after the current result set and
 * the cursor of the first record can be used to fetch records before the result set.
 *
 * NOTE: a cursor value is only valid if the `sort_by` value hasn't changed from the
 * last fetch. For example, you cannot fetch using `sort_by` id and then pass that
 * cursor value to a `sort_by` name.
 */
export type GetChannelsChannelHashedIdCollaboratorsCursor = {
  /**
   * If `cursor[enabled]` is set to 1, the first result set will be fetched with cursor pagination enabled. This
   *
   * @remarks
   * values is ignored if `cursor[before]` or `cursor[after]` are set.
   */
  enabled?: GetChannelsChannelHashedIdCollaboratorsEnabled | undefined;
  /**
   * If `cursor[before]` is set then cursor pagination is enabled and all records
   *
   * @remarks
   * before the cursor up to the `per_page` are returned. This feature is useful for
   * fetching "new records", for example, in a "pull to refersh" feature when showing records in a descending
   * order.
   */
  before?: string | undefined;
  /**
   * If `cursor[after]` is set then cursor pagination is enabled and all records
   *
   * @remarks
   * after the cursor up to the `per_page` are returned.
   */
  after?: string | undefined;
};

/**
 * Ordering. When using cursor pagination (see cursor param),
 *
 * @remarks
 * only `id` is supported.
 */
export const GetChannelsChannelHashedIdCollaboratorsSortBy = {
  Created: "created",
  Updated: "updated",
  Id: "id",
} as const;
/**
 * Ordering. When using cursor pagination (see cursor param),
 *
 * @remarks
 * only `id` is supported.
 */
export type GetChannelsChannelHashedIdCollaboratorsSortBy = ClosedEnum<
  typeof GetChannelsChannelHashedIdCollaboratorsSortBy
>;

/**
 * Ordering Sort Direction (0 = desc, 1 = asc; default is 1)
 */
export const GetChannelsChannelHashedIdCollaboratorsSortDirection = {
  Zero: 0,
  One: 1,
} as const;
/**
 * Ordering Sort Direction (0 = desc, 1 = asc; default is 1)
 */
export type GetChannelsChannelHashedIdCollaboratorsSortDirection = ClosedEnum<
  typeof GetChannelsChannelHashedIdCollaboratorsSortDirection
>;

export type GetChannelsChannelHashedIdCollaboratorsRequest = {
  /**
   * Channel Hashed ID
   */
  channelHashedId: string;
  /**
   * The page number to retrieve. This cannot be combined with `cursor`,
   *
   * @remarks
   * pagination.
   */
  page?: number | undefined;
  /**
   * The number of medias per page. Use this for both offset pagination and cursor pagination.
   */
  perPage?: number | undefined;
  /**
   * If `cursor[enabled]` is set to 1 then cursor pagination is enabled and the
   *
   * @remarks
   * first set of records are fetched up to the `per_page`. Cursor
   * pagination will also be turned on if `cursor[before]` or `cursor[after]`
   * are set. Records returned will have a `cursor` property set which can be used to fetch more records in the same `sort_by` ordering.
   * The cursor value of the last record can be used to fetch records after the current result set and
   * the cursor of the first record can be used to fetch records before the result set.
   *
   * NOTE: a cursor value is only valid if the `sort_by` value hasn't changed from the
   * last fetch. For example, you cannot fetch using `sort_by` id and then pass that
   * cursor value to a `sort_by` name.
   */
  cursor?: GetChannelsChannelHashedIdCollaboratorsCursor | undefined;
  /**
   * Ordering. When using cursor pagination (see cursor param),
   *
   * @remarks
   * only `id` is supported.
   */
  sortBy?: GetChannelsChannelHashedIdCollaboratorsSortBy | undefined;
  /**
   * Ordering Sort Direction (0 = desc, 1 = asc; default is 1)
   */
  sortDirection?:
    | GetChannelsChannelHashedIdCollaboratorsSortDirection
    | undefined;
};

/**
 * A machine-readable identifier for the specific authorization failure.
 */
export const GetChannelsChannelHashedIdCollaboratorsCode = {
  UnauthorizedCredentials: "unauthorized_credentials",
  AccountInactive: "account_inactive",
  UnauthorizedScope: "unauthorized_scope",
  UnauthorizedParams: "unauthorized_params",
} as const;
/**
 * A machine-readable identifier for the specific authorization failure.
 */
export type GetChannelsChannelHashedIdCollaboratorsCode = ClosedEnum<
  typeof GetChannelsChannelHashedIdCollaboratorsCode
>;

/**
 * The role of the collaborator on the channel.
 */
export const GetChannelsChannelHashedIdCollaboratorsRole = {
  Admin: "admin",
  Viewer: "viewer",
} as const;
/**
 * The role of the collaborator on the channel.
 */
export type GetChannelsChannelHashedIdCollaboratorsRole = ClosedEnum<
  typeof GetChannelsChannelHashedIdCollaboratorsRole
>;

/**
 * The contact or contact group being granted access.
 */
export type GetChannelsChannelHashedIdCollaboratorsShare = {
  id: number;
  name: string;
  /**
   * Either "Contact" or "ContactGroup"
   */
  type: string;
  /**
   * Email address (only present for Contact type).
   */
  email?: string | undefined;
};

/**
 * A channel collaborator is a contact or contact group that has been granted access to a channel.
 *
 * @remarks
 */
export type GetChannelsChannelHashedIdCollaboratorsResponse = {
  /**
   * The unique identifier of the channel role.
   */
  id: number;
  /**
   * The role of the collaborator on the channel.
   */
  role: GetChannelsChannelHashedIdCollaboratorsRole;
  /**
   * The timestamp when this collaborator was added.
   */
  createdAt: Date;
  /**
   * The contact or contact group being granted access.
   */
  share: GetChannelsChannelHashedIdCollaboratorsShare;
  /**
   * A cursor for stable pagination based on current `sort_by` order. You can pass this to `cursor[before]` or `cursor[after]` as a parameter to fetch the records before or after this record in the same sort order. This is only populated if records were fetched with `cursor[enabled]`, or `cursor[before]` or `cursor[after]`.
   */
  cursor?: string | null | undefined;
};

/** @internal */
export const GetChannelsChannelHashedIdCollaboratorsEnabled$outboundSchema:
  z.ZodNativeEnum<typeof GetChannelsChannelHashedIdCollaboratorsEnabled> = z
    .nativeEnum(GetChannelsChannelHashedIdCollaboratorsEnabled);

/** @internal */
export type GetChannelsChannelHashedIdCollaboratorsCursor$Outbound = {
  enabled?: number | undefined;
  before?: string | undefined;
  after?: string | undefined;
};

/** @internal */
export const GetChannelsChannelHashedIdCollaboratorsCursor$outboundSchema:
  z.ZodType<
    GetChannelsChannelHashedIdCollaboratorsCursor$Outbound,
    z.ZodTypeDef,
    GetChannelsChannelHashedIdCollaboratorsCursor
  > = z.object({
    enabled: GetChannelsChannelHashedIdCollaboratorsEnabled$outboundSchema
      .optional(),
    before: z.string().optional(),
    after: z.string().optional(),
  });

export function getChannelsChannelHashedIdCollaboratorsCursorToJSON(
  getChannelsChannelHashedIdCollaboratorsCursor:
    GetChannelsChannelHashedIdCollaboratorsCursor,
): string {
  return JSON.stringify(
    GetChannelsChannelHashedIdCollaboratorsCursor$outboundSchema.parse(
      getChannelsChannelHashedIdCollaboratorsCursor,
    ),
  );
}

/** @internal */
export const GetChannelsChannelHashedIdCollaboratorsSortBy$outboundSchema:
  z.ZodNativeEnum<typeof GetChannelsChannelHashedIdCollaboratorsSortBy> = z
    .nativeEnum(GetChannelsChannelHashedIdCollaboratorsSortBy);

/** @internal */
export const GetChannelsChannelHashedIdCollaboratorsSortDirection$outboundSchema:
  z.ZodNativeEnum<typeof GetChannelsChannelHashedIdCollaboratorsSortDirection> =
    z.nativeEnum(GetChannelsChannelHashedIdCollaboratorsSortDirection);

/** @internal */
export type GetChannelsChannelHashedIdCollaboratorsRequest$Outbound = {
  channelHashedId: string;
  page?: number | undefined;
  per_page?: number | undefined;
  cursor?: GetChannelsChannelHashedIdCollaboratorsCursor$Outbound | undefined;
  sort_by: string;
  sort_direction: number;
};

/** @internal */
export const GetChannelsChannelHashedIdCollaboratorsRequest$outboundSchema:
  z.ZodType<
    GetChannelsChannelHashedIdCollaboratorsRequest$Outbound,
    z.ZodTypeDef,
    GetChannelsChannelHashedIdCollaboratorsRequest
  > = z.object({
    channelHashedId: z.string(),
    page: z.number().int().optional(),
    perPage: z.number().int().optional(),
    cursor: z.lazy(() =>
      GetChannelsChannelHashedIdCollaboratorsCursor$outboundSchema
    ).optional(),
    sortBy: GetChannelsChannelHashedIdCollaboratorsSortBy$outboundSchema
      .default("id"),
    sortDirection:
      GetChannelsChannelHashedIdCollaboratorsSortDirection$outboundSchema
        .default(1),
  }).transform((v) => {
    return remap$(v, {
      perPage: "per_page",
      sortBy: "sort_by",
      sortDirection: "sort_direction",
    });
  });

export function getChannelsChannelHashedIdCollaboratorsRequestToJSON(
  getChannelsChannelHashedIdCollaboratorsRequest:
    GetChannelsChannelHashedIdCollaboratorsRequest,
): string {
  return JSON.stringify(
    GetChannelsChannelHashedIdCollaboratorsRequest$outboundSchema.parse(
      getChannelsChannelHashedIdCollaboratorsRequest,
    ),
  );
}

/** @internal */
export const GetChannelsChannelHashedIdCollaboratorsCode$inboundSchema:
  z.ZodNativeEnum<typeof GetChannelsChannelHashedIdCollaboratorsCode> = z
    .nativeEnum(GetChannelsChannelHashedIdCollaboratorsCode);

/** @internal */
export const GetChannelsChannelHashedIdCollaboratorsRole$inboundSchema:
  z.ZodNativeEnum<typeof GetChannelsChannelHashedIdCollaboratorsRole> = z
    .nativeEnum(GetChannelsChannelHashedIdCollaboratorsRole);

/** @internal */
export const GetChannelsChannelHashedIdCollaboratorsShare$inboundSchema:
  z.ZodType<
    GetChannelsChannelHashedIdCollaboratorsShare,
    z.ZodTypeDef,
    unknown
  > = z.object({
    id: z.number().int(),
    name: z.string(),
    type: z.string(),
    email: z.string().optional(),
  });

export function getChannelsChannelHashedIdCollaboratorsShareFromJSON(
  jsonString: string,
): SafeParseResult<
  GetChannelsChannelHashedIdCollaboratorsShare,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      GetChannelsChannelHashedIdCollaboratorsShare$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'GetChannelsChannelHashedIdCollaboratorsShare' from JSON`,
  );
}

/** @internal */
export const GetChannelsChannelHashedIdCollaboratorsResponse$inboundSchema:
  z.ZodType<
    GetChannelsChannelHashedIdCollaboratorsResponse,
    z.ZodTypeDef,
    unknown
  > = z.object({
    id: z.number().int(),
    role: GetChannelsChannelHashedIdCollaboratorsRole$inboundSchema,
    created_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    share: z.lazy(() =>
      GetChannelsChannelHashedIdCollaboratorsShare$inboundSchema
    ),
    cursor: z.nullable(z.string()).optional(),
  }).transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
    });
  });

export function getChannelsChannelHashedIdCollaboratorsResponseFromJSON(
  jsonString: string,
): SafeParseResult<
  GetChannelsChannelHashedIdCollaboratorsResponse,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      GetChannelsChannelHashedIdCollaboratorsResponse$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'GetChannelsChannelHashedIdCollaboratorsResponse' from JSON`,
  );
}
