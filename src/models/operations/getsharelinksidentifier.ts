/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GetShareLinksIdentifierRequest = {
  /**
   * The share link's URL segment — its hashed ID or custom slug.
   */
  identifier: string;
};

/**
 * A machine-readable identifier for the specific authorization failure.
 */
export const GetShareLinksIdentifierCode = {
  UnauthorizedCredentials: "unauthorized_credentials",
  AccountInactive: "account_inactive",
  UnauthorizedScope: "unauthorized_scope",
  UnauthorizedParams: "unauthorized_params",
} as const;
/**
 * A machine-readable identifier for the specific authorization failure.
 */
export type GetShareLinksIdentifierCode = ClosedEnum<
  typeof GetShareLinksIdentifierCode
>;

/**
 * Controls who can view the media via this share link.
 *
 * @remarks
 *
 * - `unlocked`: anyone with the link can view the media.
 * - `account`: only signed-in members of the media's account can view.
 * - `locked`: only contacts with access to the media's folder can view.
 * - `domain_verified`: only viewers signed in with an email address at a
 *   domain verified on the media's account can view. Requires the account
 *   to be enrolled in the domain validation gate; otherwise setting this
 *   value returns 400.
 */
export const GetShareLinksIdentifierVisibility = {
  Unlocked: "unlocked",
  Account: "account",
  Locked: "locked",
  DomainVerified: "domain_verified",
} as const;
/**
 * Controls who can view the media via this share link.
 *
 * @remarks
 *
 * - `unlocked`: anyone with the link can view the media.
 * - `account`: only signed-in members of the media's account can view.
 * - `locked`: only contacts with access to the media's folder can view.
 * - `domain_verified`: only viewers signed in with an email address at a
 *   domain verified on the media's account can view. Requires the account
 *   to be enrolled in the domain validation gate; otherwise setting this
 *   value returns 400.
 */
export type GetShareLinksIdentifierVisibility = ClosedEnum<
  typeof GetShareLinksIdentifierVisibility
>;

/**
 * A shareable link for a single media. Each media can have at most one
 *
 * @remarks
 * share link, and the link's `visibility` controls who can view the
 * media when visiting the link.
 */
export type GetShareLinksIdentifierResponse = {
  /**
   * The hashed ID of the share link. This appears in the link's URL.
   */
  id: string;
  /**
   * The hashed ID of the media this share link points to.
   */
  mediaId: string;
  /**
   * The full URL viewers can use to view the media via this share link.
   */
  url: string;
  /**
   * The share link's custom slug, if one is set. When present, the slug
   *
   * @remarks
   * replaces the hashed ID in the link's URL.
   */
  slug: string | null;
  /**
   * Controls who can view the media via this share link.
   *
   * @remarks
   *
   * - `unlocked`: anyone with the link can view the media.
   * - `account`: only signed-in members of the media's account can view.
   * - `locked`: only contacts with access to the media's folder can view.
   * - `domain_verified`: only viewers signed in with an email address at a
   *   domain verified on the media's account can view. Requires the account
   *   to be enrolled in the domain validation gate; otherwise setting this
   *   value returns 400.
   */
  visibility: GetShareLinksIdentifierVisibility;
  /**
   * When the share link was created.
   */
  createdAt: Date;
  /**
   * When the share link was last updated.
   */
  updatedAt: Date;
};

/** @internal */
export type GetShareLinksIdentifierRequest$Outbound = {
  identifier: string;
};

/** @internal */
export const GetShareLinksIdentifierRequest$outboundSchema: z.ZodType<
  GetShareLinksIdentifierRequest$Outbound,
  z.ZodTypeDef,
  GetShareLinksIdentifierRequest
> = z.object({
  identifier: z.string(),
});

export function getShareLinksIdentifierRequestToJSON(
  getShareLinksIdentifierRequest: GetShareLinksIdentifierRequest,
): string {
  return JSON.stringify(
    GetShareLinksIdentifierRequest$outboundSchema.parse(
      getShareLinksIdentifierRequest,
    ),
  );
}

/** @internal */
export const GetShareLinksIdentifierCode$inboundSchema: z.ZodNativeEnum<
  typeof GetShareLinksIdentifierCode
> = z.nativeEnum(GetShareLinksIdentifierCode);

/** @internal */
export const GetShareLinksIdentifierVisibility$inboundSchema: z.ZodNativeEnum<
  typeof GetShareLinksIdentifierVisibility
> = z.nativeEnum(GetShareLinksIdentifierVisibility);

/** @internal */
export const GetShareLinksIdentifierResponse$inboundSchema: z.ZodType<
  GetShareLinksIdentifierResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  media_id: z.string(),
  url: z.string(),
  slug: z.nullable(z.string()),
  visibility: GetShareLinksIdentifierVisibility$inboundSchema,
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  updated_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "media_id": "mediaId",
    "created_at": "createdAt",
    "updated_at": "updatedAt",
  });
});

export function getShareLinksIdentifierResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetShareLinksIdentifierResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetShareLinksIdentifierResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetShareLinksIdentifierResponse' from JSON`,
  );
}
