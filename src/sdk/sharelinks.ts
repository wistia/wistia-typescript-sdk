/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { shareLinksDelete } from "../funcs/shareLinksDelete.js";
import { shareLinksGet } from "../funcs/shareLinksGet.js";
import { shareLinksUpdate } from "../funcs/shareLinksUpdate.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class ShareLinks extends ClientSDK {
  /**
   * Show share link
   *
   * @remarks
   * Fetches the share link for a single media. If the media does not have a
   * share link yet, one is created on first request with a default
   * visibility derived from the parent folder.
   *
   * ## Requires api token with one of the following permissions
   * ```
   * Read all folder and media data
   * ```
   */
  async get(
    request: operations.GetMediasMediaIdShareLinkRequest,
    options?: RequestOptions,
  ): Promise<operations.GetMediasMediaIdShareLinkResponse> {
    return unwrapAsync(shareLinksGet(
      this,
      request,
      options,
    ));
  }

  /**
   * Update share link
   *
   * @remarks
   * Updates the share link for a single media. If the media does not have a
   * share link yet, one is created with the supplied visibility.
   *
   * ## Requires api token with one of the following permissions
   * ```
   * Read, update & delete anything
   * ```
   */
  async update(
    request: operations.PutMediasMediaIdShareLinkRequest,
    options?: RequestOptions,
  ): Promise<operations.PutMediasMediaIdShareLinkResponse> {
    return unwrapAsync(shareLinksUpdate(
      this,
      request,
      options,
    ));
  }

  /**
   * Delete share link
   *
   * @remarks
   * Deletes the share link for a media, revoking the URL. A new share link
   * can be created later with a fresh URL by calling the show or update
   * endpoint again.
   *
   * ## Requires api token with one of the following permissions
   * ```
   * Read, update & delete anything
   * ```
   */
  async delete(
    request: operations.DeleteMediasMediaIdShareLinkRequest,
    options?: RequestOptions,
  ): Promise<void> {
    return unwrapAsync(shareLinksDelete(
      this,
      request,
      options,
    ));
  }
}
