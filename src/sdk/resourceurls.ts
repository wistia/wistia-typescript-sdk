/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourceUrlsResolve } from "../funcs/resourceUrlsResolve.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class ResourceUrls extends ClientSDK {
  /**
   * Resolve Resource URLs
   *
   * @remarks
   * Resolves a resource's hashed ID and type to its canonical app URL(s) — deep
   * links an authorized user can open in the Wistia UI. The URL is only returned
   * when the authenticated user is allowed to view the resource.
   *
   * ## Requires api token with one of the following permissions
   * ```
   * Read all data
   * ```
   */
  async resolve(
    request: operations.GetResourceUrlsRequest,
    options?: RequestOptions,
  ): Promise<operations.GetResourceUrlsResponse> {
    return unwrapAsync(resourceUrlsResolve(
      this,
      request,
      options,
    ));
  }
}
