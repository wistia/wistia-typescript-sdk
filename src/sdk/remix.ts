/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { remixGetRemixAccountStatus } from "../funcs/remixGetRemixAccountStatus.js";
import { remixGetRemixesRemixHashedId } from "../funcs/remixGetRemixesRemixHashedId.js";
import { remixPostRemixes } from "../funcs/remixPostRemixes.js";
import { remixPostRemixesRemixHashedIdContinue } from "../funcs/remixPostRemixesRemixHashedIdContinue.js";
import { remixPostRemixesRemixHashedIdExport } from "../funcs/remixPostRemixesRemixHashedIdExport.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class Remix extends ClientSDK {
  /**
   * Create Remix
   *
   * @remarks
   * Start a new video remix job. The remix is processed asynchronously — poll
   * the show endpoint to check status and get preview URLs when ready.
   *
   * Remix uses AI to analyze video transcripts and create edited versions
   * (highlight reels, trailers, cut-downs, etc.) based on your instructions.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read, update & delete anything
   * ```
   * <!--- /HIDE-MCP -->
   */
  async postRemixes(
    request?: operations.PostRemixesRequest | undefined,
    options?: RequestOptions,
  ): Promise<operations.PostRemixesResponse> {
    return unwrapAsync(remixPostRemixes(
      this,
      request,
      options,
    ));
  }

  /**
   * Get Remix
   *
   * @remarks
   * Get the current status of a remix job. When the status reaches
   * "edit_tree_generated", preview URLs are included in the response.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read all folder and media data
   * ```
   * <!--- /HIDE-MCP -->
   */
  async getRemixesRemixHashedId(
    request: operations.GetRemixesRemixHashedIdRequest,
    options?: RequestOptions,
  ): Promise<operations.GetRemixesRemixHashedIdResponse> {
    return unwrapAsync(remixGetRemixesRemixHashedId(
      this,
      request,
      options,
    ));
  }

  /**
   * Continue Remix
   *
   * @remarks
   * Submit a follow-up edit to an existing remix. Creates a new remix version
   * in the same conversation. The previous remix is preserved and can be
   * referenced later.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read, update & delete anything
   * ```
   * <!--- /HIDE-MCP -->
   */
  async postRemixesRemixHashedIdContinue(
    request: operations.PostRemixesRemixHashedIdContinueRequest,
    options?: RequestOptions,
  ): Promise<operations.PostRemixesRemixHashedIdContinueResponse> {
    return unwrapAsync(remixPostRemixesRemixHashedIdContinue(
      this,
      request,
      options,
    ));
  }

  /**
   * Export Remix
   *
   * @remarks
   * Export a completed remix to a folder in your account. Triggers the full
   * render pipeline. The remix must have reached "edit_tree_generated" status.
   *
   * If no folder_id is provided, the remix is exported to the source media's folder.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read, update & delete anything
   * ```
   * <!--- /HIDE-MCP -->
   */
  async postRemixesRemixHashedIdExport(
    request: operations.PostRemixesRemixHashedIdExportRequest,
    options?: RequestOptions,
  ): Promise<operations.PostRemixesRemixHashedIdExportResponse> {
    return unwrapAsync(remixPostRemixesRemixHashedIdExport(
      this,
      request,
      options,
    ));
  }

  /**
   * Get Remix Account Status
   *
   * @remarks
   * Check the current account's remix credit usage and limits.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read all folder and media data
   * ```
   * <!--- /HIDE-MCP -->
   */
  async getRemixAccountStatus(
    options?: RequestOptions,
  ): Promise<operations.GetRemixAccountStatusResponse> {
    return unwrapAsync(remixGetRemixAccountStatus(
      this,
      options,
    ));
  }
}
