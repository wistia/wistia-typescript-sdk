/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { brandsList } from "../funcs/brandsList.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class Brands extends ClientSDK {
  /**
   * List Brands
   *
   * @remarks
   * Lists the brands belonging to the account. A brand is a saved set of
   * branding options (colors, fonts, logos, and layout) that can be applied to
   * media, projects, channels, and webinars. The account-level default brand is
   * flagged with `is_default`.
   *
   * ## Requires api token with one of the following permissions
   * ```
   * Read all data
   * ```
   */
  async list(
    request?: operations.GetBrandsRequest | undefined,
    options?: RequestOptions,
  ): Promise<Array<operations.GetBrandsResponse>> {
    return unwrapAsync(brandsList(
      this,
      request,
      options,
    ));
  }
}
