/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pushDevicesCreate } from "../funcs/pushDevicesCreate.js";
import { pushDevicesDelete } from "../funcs/pushDevicesDelete.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class PushDevices extends ClientSDK {
  /**
   * Register Push Device
   *
   * @remarks
   * Registers a push notification device token for the current contact. Used by
   * the Wistia desktop app to receive activity notifications (new comments and
   * views) for media created via the app. Re-registering an existing
   * token updates its registration in place.
   */
  async create(
    request: operations.PostDevicesRequest,
    options?: RequestOptions,
  ): Promise<operations.PostDevicesResponse> {
    return unwrapAsync(pushDevicesCreate(
      this,
      request,
      options,
    ));
  }

  /**
   * Unregister Push Device
   *
   * @remarks
   * Unregisters a push notification device token for the current contact, so no
   * further activity notifications are delivered to that device.
   */
  async delete(
    request: operations.DeleteDevicesDeviceTokenRequest,
    options?: RequestOptions,
  ): Promise<operations.DeleteDevicesDeviceTokenResponse> {
    return unwrapAsync(pushDevicesDelete(
      this,
      request,
      options,
    ));
  }
}
