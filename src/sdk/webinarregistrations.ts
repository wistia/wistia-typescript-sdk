/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webinarRegistrationsCreate } from "../funcs/webinarRegistrationsCreate.js";
import { webinarRegistrationsGetWebinarsWebinarIdRegistrations } from "../funcs/webinarRegistrationsGetWebinarsWebinarIdRegistrations.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class WebinarRegistrations extends ClientSDK {
  /**
   * List Webinar Registrations
   *
   * @remarks
   * Retrieve a paginated list of registrations for a webinar. Returns contact
   * information, attendance status, engagement metrics, and attribution data
   * for each registrant.
   *
   * Pagination uses cursor-based pagination with a `page_info` object in the
   * response rather than per-record cursors. Use `page_info.end_cursor` as
   * the `cursor` parameter to fetch the next page.
   *
   * ## Requires api token with one of the following permissions
   * ```
   * Read all data
   * ```
   */
  async getWebinarsWebinarIdRegistrations(
    request: operations.GetWebinarsWebinarIdRegistrationsRequest,
    options?: RequestOptions,
  ): Promise<operations.GetWebinarsWebinarIdRegistrationsResponse> {
    return unwrapAsync(webinarRegistrationsGetWebinarsWebinarIdRegistrations(
      this,
      request,
      options,
    ));
  }

  /**
   * Create Webinar Registration
   *
   * @remarks
   * Register a person for a webinar by providing their email, first name, and last name.
   *
   * This endpoint generates a unique visitor key and returns a personalized webinar URL for the registrant.
   *
   * ## Requires api token with one of the following permissions
   * ```
   * Read, update & delete anything
   * ```
   */
  async create(
    request: operations.PostWebinarsWebinarIdRegistrationsRequest,
    options?: RequestOptions,
  ): Promise<operations.PostWebinarsWebinarIdRegistrationsResponse> {
    return unwrapAsync(webinarRegistrationsCreate(
      this,
      request,
      options,
    ));
  }
}
