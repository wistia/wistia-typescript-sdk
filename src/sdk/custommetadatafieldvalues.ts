/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customMetadataFieldValuesDeleteMediasMediaHashedIdCustomMetadataFieldValuesKey } from "../funcs/customMetadataFieldValuesDeleteMediasMediaHashedIdCustomMetadataFieldValuesKey.js";
import { customMetadataFieldValuesGetMediasMediaHashedIdCustomMetadataFieldValues } from "../funcs/customMetadataFieldValuesGetMediasMediaHashedIdCustomMetadataFieldValues.js";
import { customMetadataFieldValuesGetMediasMediaHashedIdCustomMetadataFieldValuesKey } from "../funcs/customMetadataFieldValuesGetMediasMediaHashedIdCustomMetadataFieldValuesKey.js";
import { customMetadataFieldValuesPutMediasMediaHashedIdCustomMetadataFieldValuesKey } from "../funcs/customMetadataFieldValuesPutMediasMediaHashedIdCustomMetadataFieldValuesKey.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class CustomMetadataFieldValues extends ClientSDK {
  /**
   * List Custom Metadata Field Values
   *
   * @remarks
   * Use this endpoint to request the custom metadata field values set on a media, ordered by the field definition's position.
   *
   * Only values for active field definitions are returned. Requires the custom metadata feature to be available on your account.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read, update & delete anything
   * Read all data
   * Read all folder and media data
   * ```
   * <!--- /HIDE-MCP -->
   */
  async getMediasMediaHashedIdCustomMetadataFieldValues(
    request: operations.GetMediasMediaHashedIdCustomMetadataFieldValuesRequest,
    options?: RequestOptions,
  ): Promise<
    Array<operations.GetMediasMediaHashedIdCustomMetadataFieldValuesResponse>
  > {
    return unwrapAsync(
      customMetadataFieldValuesGetMediasMediaHashedIdCustomMetadataFieldValues(
        this,
        request,
        options,
      ),
    );
  }

  /**
   * Set Custom Metadata Field Value
   *
   * @remarks
   * Sets (or replaces) the value of a custom metadata field on a media, addressed by the field definition's immutable key. The lookup is case-insensitive.
   *
   * The request body carries a single polymorphic `value` field whose JSON type must match the definition's `field_type`:
   *
   * - text-like types (`text`, `short_text`, `url`, `email`, `money`, `time`, `datetime`) — a string; format-validated per type (e.g. money is `"USD 12.34"`, time is 24-hour `"14:30"`, datetime is UTC ISO 8601 `"2026-07-10T14:30:00Z"`)
   * - `number` — a JSON number (a numeric string is coerced)
   * - `date` — an ISO 8601 date string like `"2026-07-10"`
   * - `boolean` — a JSON boolean; `false` persists as false (it does not clear the field)
   * - `single_select` — the chosen option's key (a string); unknown option keys return a 422
   * - `multi_select` — an array of the chosen options' keys (strings); unknown option keys or a non-array value return a 422
   * - `contact_ref` — a contact reference object `{"type": "contact" | "contact_group", "id": "<hashed_id>"}`; unknown ids, ids from another account, and group references on fields that do not allow groups return a 422
   * - `contact_multi_ref` — an array of contact reference objects; the same 422 rules apply per reference, and a non-array value returns a 422
   *
   * A null or absent `value` clears the field (equivalent to the DELETE endpoint), as does an empty array for `multi_select` and `contact_multi_ref`. Type mismatches and format violations return a 422 with a field-level message.
   *
   * Only values for active field definitions can be written. Requires the custom metadata feature to be available on your account.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read, update & delete anything
   * Upload, read & update all media
   * ```
   * <!--- /HIDE-MCP -->
   */
  async putMediasMediaHashedIdCustomMetadataFieldValuesKey(
    request:
      operations.PutMediasMediaHashedIdCustomMetadataFieldValuesKeyRequest,
    options?: RequestOptions,
  ): Promise<
    | operations.PutMediasMediaHashedIdCustomMetadataFieldValuesKeyResponse
    | undefined
  > {
    return unwrapAsync(
      customMetadataFieldValuesPutMediasMediaHashedIdCustomMetadataFieldValuesKey(
        this,
        request,
        options,
      ),
    );
  }

  /**
   * Clear Custom Metadata Field Value
   *
   * @remarks
   * Clears the value of a custom metadata field on a media, addressed by the field definition's immutable key. The lookup is case-insensitive. Clearing a field that has no stored value succeeds (the operation is idempotent).
   *
   * Only values for active field definitions can be cleared. Requires the custom metadata feature to be available on your account.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read, update & delete anything
   * Upload, read & update all media
   * ```
   * <!--- /HIDE-MCP -->
   */
  async deleteMediasMediaHashedIdCustomMetadataFieldValuesKey(
    request:
      operations.DeleteMediasMediaHashedIdCustomMetadataFieldValuesKeyRequest,
    options?: RequestOptions,
  ): Promise<void> {
    return unwrapAsync(
      customMetadataFieldValuesDeleteMediasMediaHashedIdCustomMetadataFieldValuesKey(
        this,
        request,
        options,
      ),
    );
  }

  /**
   * Show Custom Metadata Field Value
   *
   * @remarks
   * Get the value of a single custom metadata field on a media, addressed by the field definition's key. The lookup is case-insensitive.
   *
   * Only values for active field definitions are returned. Requires the custom metadata feature to be available on your account.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read, update & delete anything
   * Read all data
   * Read all folder and media data
   * ```
   * <!--- /HIDE-MCP -->
   */
  async getMediasMediaHashedIdCustomMetadataFieldValuesKey(
    request:
      operations.GetMediasMediaHashedIdCustomMetadataFieldValuesKeyRequest,
    options?: RequestOptions,
  ): Promise<
    operations.GetMediasMediaHashedIdCustomMetadataFieldValuesKeyResponse
  > {
    return unwrapAsync(
      customMetadataFieldValuesGetMediasMediaHashedIdCustomMetadataFieldValuesKey(
        this,
        request,
        options,
      ),
    );
  }
}
