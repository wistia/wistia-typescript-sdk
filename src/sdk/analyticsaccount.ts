/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { analyticsAccountGet } from "../funcs/analyticsAccountGet.js";
import { analyticsAccountGetEmbedLocations } from "../funcs/analyticsAccountGetEmbedLocations.js";
import { analyticsAccountGetTimeseries } from "../funcs/analyticsAccountGetTimeseries.js";
import { analyticsAccountGetTopContent } from "../funcs/analyticsAccountGetTopContent.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class AnalyticsAccount extends ClientSDK {
  /**
   * Show Account Analytics
   *
   * @remarks
   * Retrieve aggregate analytics for the entire account over a date range. This endpoint
   * provides Bottler-powered analytics across all of the account's media including plays,
   * loads, engagement rate, play rate, and conversion metrics.
   *
   * The date range between `start_date` and `end_date` must not exceed 2 years.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read detailed stats
   * ```
   * <!--- /HIDE-MCP -->
   */
  async get(
    request: operations.GetAnalyticsAccountRequest,
    options?: RequestOptions,
  ): Promise<operations.GetAnalyticsAccountResponse> {
    return unwrapAsync(analyticsAccountGet(
      this,
      request,
      options,
    ));
  }

  /**
   * Show Account Analytics Timeseries
   *
   * @remarks
   * Retrieve analytics timeseries data for the entire account over a date range with
   * configurable granularity. Returns an array of timestamped metric buckets aggregated
   * across all of the account's media.
   *
   * The date range between `start_date` and `end_date` must not exceed 2 years.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read detailed stats
   * ```
   * <!--- /HIDE-MCP -->
   */
  async getTimeseries(
    request: operations.GetAnalyticsAccountTimeseriesRequest,
    options?: RequestOptions,
  ): Promise<Array<operations.GetAnalyticsAccountTimeseriesResponse>> {
    return unwrapAsync(analyticsAccountGetTimeseries(
      this,
      request,
      options,
    ));
  }

  /**
   * Show Account Top Content
   *
   * @remarks
   * Rank the account's content by a chosen metric over a date range. Returns the top
   * media, channels, or folders (controlled by `group_by`) with their analytics,
   * answering questions like "what were my most-played videos last month?".
   *
   * Optionally pass `hashed_ids` to scope the ranking to a specific set of media
   * instead of the whole account — useful for fetching analytics for a known list
   * of videos, still sorted by `sort_by`.
   *
   * The date range between `start_date` and `end_date` must not exceed 2 years.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read detailed stats
   * ```
   * <!--- /HIDE-MCP -->
   */
  async getTopContent(
    request: operations.GetAnalyticsAccountTopContentRequest,
    options?: RequestOptions,
  ): Promise<Array<operations.GetAnalyticsAccountTopContentResponse>> {
    return unwrapAsync(analyticsAccountGetTopContent(
      this,
      request,
      options,
    ));
  }

  /**
   * Show Account Embed Locations
   *
   * @remarks
   * Retrieve embed location analytics for the entire account. Returns a list of domains
   * where the account's media are embedded, ranked by the chosen metric.
   *
   * The date range between `start_date` and `end_date` must not exceed 2 years.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read detailed stats
   * ```
   * <!--- /HIDE-MCP -->
   */
  async getEmbedLocations(
    request: operations.GetAnalyticsAccountEmbedLocationsRequest,
    options?: RequestOptions,
  ): Promise<Array<operations.GetAnalyticsAccountEmbedLocationsResponse>> {
    return unwrapAsync(analyticsAccountGetEmbedLocations(
      this,
      request,
      options,
    ));
  }
}
