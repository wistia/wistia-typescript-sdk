/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deletedMediaGetDeletedMedia } from "../funcs/deletedMediaGetDeletedMedia.js";
import { deletedMediaPostDeletedMediaRestore } from "../funcs/deletedMediaPostDeletedMediaRestore.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class DeletedMedia extends ClientSDK {
  /**
   * List Deleted Media
   *
   * @remarks
   * Lists media that has been soft-deleted and is still inside the account's
   * restore window. Media is listed only while it can still be restored — 30 days
   * on most plans, 14 on free plans. After which it is permanently purged.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Read all folder and media data
   * ```
   * <!--- /HIDE-MCP -->
   */
  async getDeletedMedia(
    request?: operations.GetDeletedMediaRequest | undefined,
    options?: RequestOptions,
  ): Promise<Array<operations.GetDeletedMediaResponse>> {
    return unwrapAsync(deletedMediaGetDeletedMedia(
      this,
      request,
      options,
    ));
  }

  /**
   * Restore Deleted Media
   *
   * @remarks
   * Restores one or more soft-deleted media. By default each media returns to the
   * folder it was deleted from; pass folder_id to restore them into a specific
   * folder instead. Only media still inside the restore window can be recovered.
   * The restore runs asynchronously and the response includes a background job
   * status.
   *
   * <!--- HIDE-MCP -->
   * ## Requires api token with one of the following permissions
   * ```
   * Upload and view media
   * ```
   * <!--- /HIDE-MCP -->
   */
  async postDeletedMediaRestore(
    request: operations.PostDeletedMediaRestoreRequest,
    options?: RequestOptions,
  ): Promise<operations.PostDeletedMediaRestoreResponse> {
    return unwrapAsync(deletedMediaPostDeletedMediaRestore(
      this,
      request,
      options,
    ));
  }
}
