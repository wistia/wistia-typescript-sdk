/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { WistiaCore } from "../core.js";
import {
  encodeDeepObjectQuery,
  encodeFormQuery,
  queryJoin,
} from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import { WistiaError } from "../models/errors/wistiaerror.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * List Deleted Media
 *
 * @remarks
 * Lists media that has been soft-deleted and is still inside the account's
 * restore window. Media is listed only while it can still be restored — 30 days
 * on most plans, 14 on free plans. After which it is permanently purged.
 *
 * <!--- HIDE-MCP -->
 * ## Requires api token with one of the following permissions
 * ```
 * Read all folder and media data
 * ```
 * <!--- /HIDE-MCP -->
 */
export function deletedMediaGetDeletedMedia(
  client: WistiaCore,
  request?: operations.GetDeletedMediaRequest | undefined,
  options?: RequestOptions,
): APIPromise<
  Result<
    Array<operations.GetDeletedMediaResponse>,
    | errors.GetDeletedMediaBadRequestError
    | errors.GetDeletedMediaUnauthorizedError
    | errors.GetDeletedMediaForbiddenError
    | errors.GetDeletedMediaInternalServerError
    | WistiaError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    request,
    options,
  ));
}

async function $do(
  client: WistiaCore,
  request?: operations.GetDeletedMediaRequest | undefined,
  options?: RequestOptions,
): Promise<
  [
    Result<
      Array<operations.GetDeletedMediaResponse>,
      | errors.GetDeletedMediaBadRequestError
      | errors.GetDeletedMediaUnauthorizedError
      | errors.GetDeletedMediaForbiddenError
      | errors.GetDeletedMediaInternalServerError
      | WistiaError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) =>
      operations.GetDeletedMediaRequest$outboundSchema.optional().parse(value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const path = pathToFunc("/deleted_media")();

  const query = queryJoin(
    encodeDeepObjectQuery({
      "cursor": payload?.cursor,
    }),
    encodeFormQuery({
      "hashed_ids[]": payload?.["hashed_ids[]"],
      "page": payload?.page,
      "per_page": payload?.per_page,
      "sort_by": payload?.sort_by,
      "sort_direction": payload?.sort_direction,
    }),
  );

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const secConfig = await extractSecurity(client._options.bearerAuth);
  const securityInput = secConfig == null ? {} : { bearerAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "get_/deleted_media",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.bearerAuth,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    Array<operations.GetDeletedMediaResponse>,
    | errors.GetDeletedMediaBadRequestError
    | errors.GetDeletedMediaUnauthorizedError
    | errors.GetDeletedMediaForbiddenError
    | errors.GetDeletedMediaInternalServerError
    | WistiaError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, z.array(operations.GetDeletedMediaResponse$inboundSchema)),
    M.jsonErr(400, errors.GetDeletedMediaBadRequestError$inboundSchema),
    M.jsonErr(401, errors.GetDeletedMediaUnauthorizedError$inboundSchema),
    M.jsonErr(403, errors.GetDeletedMediaForbiddenError$inboundSchema),
    M.jsonErr(500, errors.GetDeletedMediaInternalServerError$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
