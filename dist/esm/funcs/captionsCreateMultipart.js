/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import * as z from "zod";
import { appendForm, encodeSimple } from "../lib/encodings.js";
import { getContentTypeFromFileName, readableStreamToArrayBuffer, } from "../lib/files.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import * as errors from "../models/errors/index.js";
import * as operations from "../models/operations/index.js";
import { APIPromise } from "../types/async.js";
import { isBlobLike } from "../types/blobs.js";
import { isReadableStream } from "../types/streams.js";
/**
 * Captions Create
 *
 * @remarks
 * Adds captions to a specified video by providing an SRT file or its contents directly.
 *
 * ## Requires api token with one of the following permissions
 * ```
 * Read, update & delete anything
 * ```
 */
export function captionsCreateMultipart(client, request, options) {
    return new APIPromise($do(client, request, options));
}
async function $do(client, request, options) {
    const parsed = safeParse(request, (value) => operations.PostMediasMediaHashedIdCaptionsMultipartRequest$outboundSchema
        .parse(value), "Input validation failed");
    if (!parsed.ok) {
        return [parsed, { status: "invalid" }];
    }
    const payload = parsed.value;
    const body = new FormData();
    if (isBlobLike(payload.RequestBody.caption_file)) {
        appendForm(body, "caption_file", payload.RequestBody.caption_file);
    }
    else if (isReadableStream(payload.RequestBody.caption_file.content)) {
        const buffer = await readableStreamToArrayBuffer(payload.RequestBody.caption_file.content);
        const contentType = getContentTypeFromFileName(payload.RequestBody.caption_file.fileName)
            || "application/octet-stream";
        const blob = new Blob([buffer], { type: contentType });
        appendForm(body, "caption_file", blob, payload.RequestBody.caption_file.fileName);
    }
    else {
        const contentType = getContentTypeFromFileName(payload.RequestBody.caption_file.fileName)
            || "application/octet-stream";
        appendForm(body, "caption_file", new Blob([payload.RequestBody.caption_file.content], {
            type: contentType,
        }), payload.RequestBody.caption_file.fileName);
    }
    if (payload.RequestBody.language !== undefined) {
        appendForm(body, "language", payload.RequestBody.language);
    }
    const pathParams = {
        mediaHashedId: encodeSimple("mediaHashedId", payload.mediaHashedId, {
            explode: false,
            charEncoding: "percent",
        }),
    };
    const path = pathToFunc("/medias/{mediaHashedId}/captions")(pathParams);
    const headers = new Headers(compactMap({
        Accept: "application/json",
    }));
    const secConfig = await extractSecurity(client._options.bearerAuth);
    const securityInput = secConfig == null ? {} : { bearerAuth: secConfig };
    const requestSecurity = resolveGlobalSecurity(securityInput);
    const context = {
        options: client._options,
        baseURL: options?.serverURL ?? client._baseURL ?? "",
        operationID: "post_/medias/{mediaHashedId}/captions_multipart",
        oAuth2Scopes: [],
        resolvedSecurity: requestSecurity,
        securitySource: client._options.bearerAuth,
        retryConfig: options?.retries
            || client._options.retryConfig
            || { strategy: "none" },
        retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
    };
    const requestRes = client._createRequest(context, {
        security: requestSecurity,
        method: "POST",
        baseURL: options?.serverURL,
        path: path,
        headers: headers,
        body: body,
        userAgent: client._options.userAgent,
        timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
    }, options);
    if (!requestRes.ok) {
        return [requestRes, { status: "invalid" }];
    }
    const req = requestRes.value;
    const doResult = await client._do(req, {
        context,
        errorCodes: ["400", "401", "404", "4XX", "500", "5XX"],
        retryConfig: context.retryConfig,
        retryCodes: context.retryCodes,
    });
    if (!doResult.ok) {
        return [doResult, { status: "request-error", request: req }];
    }
    const response = doResult.value;
    const responseFields = {
        HttpMeta: { Response: response, Request: req },
    };
    const [result] = await M.match(M.nil(200, z.void()), M.jsonErr(401, errors
        .PostMediasMediaHashedIdCaptionsMultipartUnauthorizedError$inboundSchema), M.jsonErr(500, errors
        .PostMediasMediaHashedIdCaptionsMultipartInternalServerError$inboundSchema), M.fail([400, 404, "4XX"]), M.fail("5XX"))(response, req, { extraFields: responseFields });
    if (!result.ok) {
        return [result, { status: "complete", request: req, response }];
    }
    return [result, { status: "complete", request: req, response }];
}
//# sourceMappingURL=captionsCreateMultipart.js.map