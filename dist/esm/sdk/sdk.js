/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { ClientSDK } from "../lib/sdks.js";
import { Account } from "./account.js";
import { AllowedDomains } from "./alloweddomains.js";
import { BackgroundJobStatus } from "./backgroundjobstatus.js";
import { Captions } from "./captions.js";
import { ChannelEpisodes } from "./channelepisodes.js";
import { Channels } from "./channels.js";
import { Customizations } from "./customizations.js";
import { ExpiringAccessTokens } from "./expiringaccesstokens.js";
import { LiveStreamEvents } from "./livestreamevents.js";
import { Localizations } from "./localizations.js";
import { Media } from "./media.js";
import { Projects } from "./projects.js";
import { ProjectSharings } from "./projectsharings.js";
import { Search } from "./search.js";
import { StatsAccount } from "./statsaccount.js";
import { StatsEvents } from "./statsevents.js";
import { StatsMedia } from "./statsmedia.js";
import { StatsProjects } from "./statsprojects.js";
import { StatsVisitors } from "./statsvisitors.js";
import { Subfolders } from "./subfolders.js";
import { Tags } from "./tags.js";
import { Trims } from "./trims.js";
export class Wistia extends ClientSDK {
    get media() {
        return (this._media ?? (this._media = new Media(this._options)));
    }
    get projects() {
        return (this._projects ?? (this._projects = new Projects(this._options)));
    }
    get subfolders() {
        return (this._subfolders ?? (this._subfolders = new Subfolders(this._options)));
    }
    get projectSharings() {
        return (this._projectSharings ?? (this._projectSharings = new ProjectSharings(this._options)));
    }
    get account() {
        return (this._account ?? (this._account = new Account(this._options)));
    }
    get allowedDomains() {
        return (this._allowedDomains ?? (this._allowedDomains = new AllowedDomains(this._options)));
    }
    get backgroundJobStatus() {
        return (this._backgroundJobStatus ?? (this._backgroundJobStatus = new BackgroundJobStatus(this._options)));
    }
    get customizations() {
        return (this._customizations ?? (this._customizations = new Customizations(this._options)));
    }
    get captions() {
        return (this._captions ?? (this._captions = new Captions(this._options)));
    }
    get trims() {
        return (this._trims ?? (this._trims = new Trims(this._options)));
    }
    get localizations() {
        return (this._localizations ?? (this._localizations = new Localizations(this._options)));
    }
    get tags() {
        return (this._tags ?? (this._tags = new Tags(this._options)));
    }
    get search() {
        return (this._search ?? (this._search = new Search(this._options)));
    }
    get channels() {
        return (this._channels ?? (this._channels = new Channels(this._options)));
    }
    get channelEpisodes() {
        return (this._channelEpisodes ?? (this._channelEpisodes = new ChannelEpisodes(this._options)));
    }
    get expiringAccessTokens() {
        return (this._expiringAccessTokens ?? (this._expiringAccessTokens = new ExpiringAccessTokens(this._options)));
    }
    get liveStreamEvents() {
        return (this._liveStreamEvents ?? (this._liveStreamEvents = new LiveStreamEvents(this._options)));
    }
    get statsAccount() {
        return (this._statsAccount ?? (this._statsAccount = new StatsAccount(this._options)));
    }
    get statsProjects() {
        return (this._statsProjects ?? (this._statsProjects = new StatsProjects(this._options)));
    }
    get statsMedia() {
        return (this._statsMedia ?? (this._statsMedia = new StatsMedia(this._options)));
    }
    get statsVisitors() {
        return (this._statsVisitors ?? (this._statsVisitors = new StatsVisitors(this._options)));
    }
    get statsEvents() {
        return (this._statsEvents ?? (this._statsEvents = new StatsEvents(this._options)));
    }
}
//# sourceMappingURL=sdk.js.map