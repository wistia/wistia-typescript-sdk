# PostMediasMediaHashedIdCaptionsLanguageCodeEditsConflictError

The version guard failed. For `stale_version`, re-read and retry with the
active version. For `diverged`, newer inactive versions exist, so
re-reading will not help; wait and retry, then escalate if it persists.


## Example Usage

```typescript
import { PostMediasMediaHashedIdCaptionsLanguageCodeEditsConflictError } from "@wistia/wistia-api-client/models/errors";

// No examples available for this model
```

## Fields

| Field                                                                  | Type                                                                   | Required                                                               | Description                                                            |
| ---------------------------------------------------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| `reason`                                                               | [operations.ConflictReason](../../models/operations/conflictreason.md) | :heavy_check_mark:                                                     | N/A                                                                    |
| `errors`                                                               | *string*[]                                                             | :heavy_check_mark:                                                     | N/A                                                                    |